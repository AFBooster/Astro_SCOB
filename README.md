# SCOB Night-Sky Dashboard — Singapore Science Centre Observatory

A single-file web app for planning the Friday-night stargazing sessions at the Science Centre Observatory (SCOB), Singapore (1.3342° N, 103.7357° E). Everything — moon, planets, deep-sky target altitudes, sky map, sunset/twilight times — is computed on-device, so the page works offline; only the live weather forecast needs a connection.

**File to open or host: `scob-dashboard-v3.html`** — the single canonical file. `scob-dashboard.html` and `main.html` are now just redirect stubs to it (for anyone who installed the PWA from those URLs); superseded versions were removed during housekeeping and remain in git history if ever needed.

## Features

### Date & time planning
- Defaults to the **next Friday, 8:30 pm SGT** (session night), with a live countdown to the chosen moment.
- Date picker, time picker, and a drag **time slider** to scrub through the night; **● Now** and **↺ Next Fri** shortcuts.
- Flags whether the chosen date is a Friday session night.

### Tonight at a glance *(v3)*
- One-line **go / no-go verdict** combining the live forecast and sky darkness.
- Chips for **sunset, fully-dark (astronomical twilight end), sunrise**, moon % and altitude, **best planet**, and the **top deep-sky target** right now.

### Sky map
- All-sky "looking up" chart (N top, E left) with planets, Moon, Sun, deep-sky targets, and 17 constellation stick figures (tap the map to toggle them).
- Hover / tap any object for an illustrated tooltip explaining what it is.

### Moon
- Phase name, % illuminated, graphical disc, rise/set times, and practical notes on how much moonlight will wash out faint targets.

### Solar system
- Sun, Moon, and all seven planets: altitude, compass direction, magnitude, and a visibility verdict (naked eye / binoculars / scope / twilight / below horizon).

### Deep-sky targets
- Messier + Caldwell selection ranked by altitude, with magnitude, compass direction, and **Moon separation** for each.
- Two modes, remembered between visits:
  - **👁 Visual** — eyepiece verdicts (Excellent / Good / Low / Washed out under a bright Moon).
  - **📷 Seestar S30 Pro** — imaging verdicts that account for the dual-band Hα/OIII filter (emission nebulae stay imageable under moonlight; broadband galaxies flagged for dark weeks).

### Weather — go / no-go
- **Auto-loading live forecast** (Open-Meteo) for the chosen date & time: cloud cover, rain chance, temperature, humidity, wind, and a GO / CAUTION / NO-GO verdict. *(auto-load: v3)*
- **Evening cloud timeline** *(v3)* — hour-by-hour bars from 6 pm to 11 pm with the **clearest observing window highlighted**.
- Seasonal monsoon guidance for dates beyond the ~16-day forecast window, plus quick links to the **MSS 2-hr Nowcast** and SCOB Facebook for the on-the-night call.

### Special events *(v3.2)*
- Verified 2026–27 calendar of **meteor showers, eclipses, oppositions, elongations & supermoons**, flagged for the 3 weeks after the chosen date, with Singapore-visibility notes (events not visible from SG are dimmed). Works fully offline; the chosen night's event also appears as a glance chip.

### ISS passes *(v3.2)*
- Evening (7–11 pm) **ISS pass predictions** computed on-device from a CelesTrak TLE (cached in the browser, refreshed every ~3 days when online) via satellite.js — rise/peak/set times, direction, max altitude, and whether the pass is sunlit (visible) or in Earth's shadow. Best visible pass shows as a glance chip.

### Haze / PSI *(v3.2)*
- **Live NEA 24-h PSI (west region)** in the weather card with a transparency note — Good / Moderate / Unhealthy.

### Session run-sheet *(v3.2)*
- Auto-generated **per-instrument plan at 7:30 / 8:30 / 9:30 pm**: Moon → deck CPC/C6, best planets → 40 cm & 11" domes, globulars/clusters → 40 cm + 6" apo, emission nebulae → Seestar S30 dual-band.

### 📄 Briefing *(v3.2)*
- One tap builds a **print-friendly weekly briefing** (glance verdict, weather, Moon, planets, top-10 targets, events, ISS, run-sheet, sources) — print or save as PDF straight from the browser.

### 🌕 Tonight on the Moon *(v3.3)*
A dedicated page, **`moon-tonight.html`** (linked from the Moon card and the footer):

- Drawn **Moon disc with the computed terminator**, maria and craters, for 8:30 pm on any chosen date.
- **"On the terminator tonight"** — craters, rilles, scarps & mountain ranges within ~13° of the sunrise/sunset line, ranked, with one-line docent descriptions and scope tips (35-feature IAU/USGS-based catalogue, incl. Tranquility Base).
- Full-Moon nights switch automatically to the **ray systems** story (Tycho, Copernicus, Kepler, Aristarchus); New-Moon nights point back to deep-sky.
- Phase, age, colongitude, rise/set and an **up-during-session verdict**; earthshine tip on thin crescents; night mode; cached offline.

### Jupiter & Saturn eyepiece detail *(v3.3)*
- **Galilean moon positions** drawn to scale for the chosen time, flagging transits/occultations.
- **Great Red Spot meridian-crossing times** for the evening (Meeus CM series + JUPOS drift trend).
- **Saturn ring tilt** computed on-device with an opening/edge-on interpretation.

### Double & carbon stars *(v3.3)*
- 18 moon-proof showpieces (Albireo, α Cen, Acrux, the Double-Double, La Superba, DY Crucis…) with mags, separations, colours and altitude-ranked verdicts — the go-to list when moonlight washes out the fuzzies.

### Conjunction finder *(v3.3)*
- **Moon–planet (<5°) and planet–planet (<2.5°) pairings computed on-device** for 8:30 pm over the 3 weeks after the chosen date — no longer limited to the static events calendar. Tonight's pairing appears as a glance chip.

### Rain radar *(v3.3)*
- **Live MSS 50-km rain-radar image** embedded in the weather card (5-min timestamps, auto-fallback), alongside the existing forecast, cloud timeline and PSI.

### Satellite passes *(v3.3, upgrades v3.2 ISS)*
- Evening passes now cover the **ISS, Tiangong and Hubble** (CelesTrak TLEs, computed on-device), labelled per satellite with visibility flags.

### Telescope fleet reference
- SCOB main dome 40 cm (16") Cassegrain + 15 cm (6") apo, second dome 11" + 7", deck Celestron CPC (8"–11") + C6 — with tips on which instrument suits which target class.

### Telescope guide page *(v3.1)*
A companion page, **`telescope-types.html`**, cross-linked with the dashboard (button in the dashboard footer; back-links in the guide's header and footer):

- **Five optical designs with animated light paths** — refractor, Newtonian reflector, classical Cassegrain, Schmidt-Cassegrain and Maksutov-Cassegrain — each SVG shows photons travelling through the lenses/mirrors and notes which SCOB instrument uses that design.
- **Four mount types with animated illustrations** — alt-azimuth GoTo fork (CPC), Dobsonian, German equatorial, and the English yoke carrying the 40 cm main scope — with typical makes and usage.
- **DSO tracking explainer** — animated comparison of equatorial (one axis at 15°/hr) vs alt-az GoTo (both axes recomputed every second) tracking, including field rotation and the Singapore quirk of a celestial pole on the horizon.
- **Magnification calculator** — magnification = focal length ÷ eyepiece, with one-tap presets for the whole SCOB fleet, f-ratio, exit pupil, max/min useful magnification warnings, and a live table of common eyepieces.
- Same **Auto / Desktop / Mobile** switcher as the dashboard (shares the saved choice) and cached by the service worker for offline use.

### App conveniences
- **Desktop and mobile layouts** in one file, auto-detected (manual override available).
- **Night mode** (red-tinted, dark-adaptation friendly), remembered between visits.
- Installable as a **home-screen / PWA app** (`manifest.webmanifest`, `sw.js`, icons — see `INSTALL-as-app.md`).

## Version history

| Version | Files | What changed |
|---|---|---|
| **v4.04** *(current)* | `scob-dashboard-v3.html`, `sw.js`, `version.js` | **Fixed the desktop dashboard&rsquo;s scroll dead-band.** On wide desktop screens, scrolling down sometimes left a tall blank gap between the collapsed date/time/scrubber bar and the sky map. The header&rsquo;s collapse-on-scroll was driven by JavaScript juggling inline `max-height` values plus `transitionend` events; because collapsing changes the page height and re-fires the scroll handler, the state could **desync** — the header carried the minimised class while its groups stayed expanded, so it rendered ~390&nbsp;px tall (title/glance area) with nothing in it. The collapsed vs open state is now driven **entirely by the `.hdr-min` CSS class** (class → rendered height, no inline juggling, no `transitionend` race), so the two can never disagree; JS only measures each group&rsquo;s real height into a `--openH` custom property (kept fresh via a `MutationObserver`) so the slide stays smooth and async header content never clips. Verified in a real browser at 1900&nbsp;px: the collapsed header is now ~106&nbsp;px (was ~393&nbsp;px), with content flush beneath it. Desktop-only; the mobile slim bar was never affected. cache -> `scob-sky-v107`. |
| **v4.03** | `index.html` (new), `site-map.html` (new), `first-night.html` (new), `satellite.min.js` (new), `qrcode.min.js` (new), `test-browser.js` (new, dev-only), `scob-dashboard-v3.html`, `session-log.html`, `sky-guide.html`, `about.html`, `.github/workflows/deploy-pages.yml`, `check-release.sh`, `sw.js`, `version.js` | **Robustness, discoverability & calibration — five improvements shipped together.** (1) **Fully offline, first load included.** The two third-party libraries the app fetched from cdnjs at runtime — **satellite.js** (ISS/Tiangong/Hubble passes) and **qrcode.js** (kiosk/object-card/poster QR) — are now **self-hosted** and precached, so a fresh PWA install works with no signal on the observatory deck; the six pages that used them were repointed and the About credits updated. (2) **A committed `index.html`** root entry point (redirect to the dashboard; links onward to the public Sky Guide and the new site map). (3) **`site-map.html` (new)** — a single grouped index of every page with a live **client-side search** over titles and descriptions (66 pages, three groups), linked from the dashboard ☰ Reference guides, the Sky Guide footer and the root. (4) **`first-night.html` (new)** — a new-volunteer quickstart that stitches the existing pieces into one linear flow: tonight’s go/no-go, the top three things to show right now (with where to point and which SCOB scope), and the three deck rules — computed on-device, linked at the top of the ☰ Tonight’s session menu. (5) **`test-browser.js` (new)** — a real headless-Chromium smoke test (Playwright) that loads every page with external network blocked and fails on an uncaught error or a blank render, catching the rendering/offline class of bug the stub-DOM suites structurally cannot; wired into the deploy workflow as a gate (and stripped from the published site). Plus: an optional **all-sky camera** tile on the dashboard weather card, inert until you set the `ALLSKY_CAM` URL constant; and a **forecast-vs-reality** readout on the session logbook (it captures the go/no-go the app would have given each night and grades it against the conditions you log, so the call learns our site over a season). All four gates plus the new browser test pass. cache -> `scob-sky-v106`. |
| **v4.02** | `solar-system.html`, `sw.js`, `version.js` | **Housekeeping — button-type consistency.** A deep audit of the site (link/stub integrity, page conventions, security, dead code, offline-cache coverage, duplicate ids) came back clean except for one cosmetic deviation: `solar-system.html` carried **9 `<button>` elements without an explicit `type="button"`**. With no `<form>` on the page there was no functional impact, but it broke the project convention (every interactive button carries `type="button"` since v3.42), so all nine were given `type="button"`. No behaviour or layout change. cache -> `scob-sky-v105`. |
| **v4.01** | `hemispheres.html` (new), `sky-guide.html`, `scob-dashboard-v3.html`, `about.html`, `sw.js`, `version.js` | **New visitor explainer — northern vs southern hemisphere skies.** `hemispheres.html` answers why a stargazer in London and one in Sydney never see the same night sky: the two celestial poles, circumpolar stars, the seasonal flip, Polaris versus the Southern Cross, and why Orion and the Moon appear upside down down south — then ties it back to Singapore sitting on the equator, where both halves come past us over a year. Computed live on-device (a declination-band chart across a northern site / SCOB / a southern site; the two pole stars; signature-constellation lists that flag whether each is up **right now** over Singapore; a 12-object comparison table across 45°N / SCOB / 45°S with a live "now" column). A companion to `singapore-sky.html`, styled with the shared theme and driven by the shared `astro-core.js` engine (`altazAt` / `compass`). Linked from the public **Sky Guide** (four-language tile) and the dashboard ☰ **Explore &amp; share** menu. Also added an **Explore &amp; understand the sky** section to the **About** page, indexing the equatorial / light-pollution explainer family — `singapore-sky.html`, `hemispheres.html` and `light-pollution.html` — which weren't previously linked from there. cache -> `scob-sky-v104`. |
| **v4.0** | `about.html`, `sw.js`, `version.js` | **Milestone + housekeeping.** Rolled the version to **v4.0** to mark the completed work of the v3.7x–v3.9x cycle: the **11-culture** cultural strand plus its two synthesis pages (Three Lunar-Mansion Systems, Sky Through the Ages), the **session/foyer toolset** (Now Showing board, session reminder, logbook, visitor passport, foyer display), the **four-language** rollout, per-target tour timings, Moon-aware Seestar picks, and the new **eclipses** page. Housekeeping: credited the eclipse data source (NASA GSFC / timeanddate) and the two synthesis pages on the **About** page; full spot-check across the live site and all four gates (`test-astro`, `test-pages`, `test-logic`, `check-release`) green. No behaviour change. cache -> `scob-sky-v103`. |
| **v3.98** | `eclipses.html`, `sw.js`, `version.js` | **Copy tidy on the eclipses page.** The Singapore visibility line no longer repeats its own badge (was e.g. “Not visible from Singapore — Not visible from Singapore …”); each note now adds only the extra detail (or nothing) after the badge. No logic change. cache -> `scob-sky-v102`. |
| **v3.97** | `eclipses.html` (new), `scob-dashboard-v3.html`, `sky-guide.html`, `sw.js`, `version.js` | **New page — eclipses, solar &amp; lunar.** Explains the geometry (why a shadow, why not every month, the 5° tilt, nodes, the saros) and all the eclipse types, with a prominent solar-viewing **safety** warning. A **2026–2030 forecast** of every solar and lunar eclipse (filter by solar / lunar / seen-from-Singapore), each with type, on-device countdown, the region it crosses, a Singapore-visibility note, and a per-eclipse link to NASA/timeanddate. A **schematic world map** traces each solar eclipse's path of totality/annularity across the Earth (Singapore marked), with a note that a lunar eclipse is seen from the whole night side. Honest Singapore guidance: no total/annular solar crosses Singapore in this window (the 2 Aug 2027 total is a travel eclipse), so the realistic SCOB events are the **total lunar eclipses** (notably 31 Dec 2028). Sourced to NASA GSFC & timeanddate. Linked from Explore &amp; the Sky Guide. cache -> `scob-sky-v101`. |
| **v3.96** | `scob-dashboard-v3.html`, `sw.js`, `version.js` | **Menu tidy, part 2 — split the crowded session group and gave culture ‘tonight’ views their own collapsed section.** ‘Tonight's session’ now holds just the four docent/observing tools (Tour, Point-your-phone, Moon tonight, Comets); a new **‘Foyer displays &amp; backstage (volunteers)’** section holds the Now Showing board, Foyer display, kiosk, passport, reminder and logbook. The 11 culture ‘tonight’ pages moved off the flat list into a dedicated **collapsed ‘Culture skies — tonight views’** section (all still on the dashboard, one click to expand) — so nothing left the menu, but every group is now short. Reuses the existing collapse/persist/filter machinery (new `data-sec` sections auto-wire, default collapsed). No new files. cache -> `scob-sky-v100`. |
| **v3.95** | `scob-dashboard-v3.html`, `sky-guide.html`, `sw.js`, `version.js` | **Menu tidy — shorter ‘Skies across cultures’ section.** Put the two synthesis pages (🕰️ Sky Through the Ages, ☾ Three Lunar-Mansion Systems) at the top of the dashboard's culture menu, and collapsed each of the 11 cultures from two links to one (the explainer page; its ‘tonight’ view is one tap away inside the page). Added a single ‘…and every tonight view → Sky Guide’ pointer, and — filling a gap — added the missing **arabic-tonight** tile to the Sky Guide's ‘See the sky tonight’ list, so all 11 culture ‘tonight’ pages are now there in four languages. Section went from 24 links to 14. No new files. cache -> `scob-sky-v99`. |
| **v3.94** | `sky-through-ages.html`, `session-log.html`, `passport.html`, `presentation.html` (all new), `object-cards.html`, `moon-tonight.html`, `tonights-tour.html`, `scob-dashboard-v3.html`, `sky-guide.html`, `sw.js`, `version.js` | **Cultural capstone, session logbook, passport, foyer display, wider four-language, and per-target timings.** New `sky-through-ages.html` puts all 11 sky cultures on one timeline with a live 'one star, every age' explorer (via `crossLinkList`) and the Babylon→Greece→Arabia→IAU naming lineage. New `session-log.html` — a private on-device logbook (conditions, objects, attendance, notes) with CSV export and one-tap seeding from the Now Showing board. New `passport.html` — an on-screen + printable visitor stamp card tied to the showpieces. New `presentation.html` — a high-contrast, auto-advancing foyer display of what's up now. Four-language toggle extended to `object-cards.html` (full) and `moon-tonight.html` (chrome + phase/up-chip). `tonights-tour.html` now shows a per-stop **altitude-vs-time sparkline** for the 7:30–10pm window (peak + current time marked). Wired into the dashboard and Sky Guide. cache -> `scob-sky-v98`. |
| **v3.93** | `arabic-sky.html`, `arabic-tonight.html`, `arabic-sky-data.js`, `lunar-mansions.html`, `now-showing.html`, `session-reminder.html` (all new), `sky-culture-core.js`, `scob-dashboard-v3.html`, `sky-guide.html`, `about.html`, `seestar.html`, `highlights-card.html`, `tonights-tour.html`, `sw.js`, `version.js` | **Arabic sky (28 manāzil) + comparative lunar-mansion page, plus visitor/session features.** New `arabic-sky.html`/`arabic-tonight.html`/`arabic-sky-data.js`: the 28 Arabic lunar mansions (11th culture registered in `sky-culture-core.js`), with live Chinese-mansion & Indian-nakshatra cross-links and the Moon's own manzil from its live ecliptic longitude. New `lunar-mansions.html` matches Chinese 宿 / Indian nakshatra / Arabic manzil star-by-star (Antares = 心 = Jyeṣṭhā = al-Qalb). New `now-showing.html` (per-scope Now/Next board for the foyer) and `session-reminder.html` (in-app Notifications reminder + countdown for the next Friday session). Four-language toggle (EN/中文/BM/Tamil) on `highlights-card.html` and `tonights-tour.html`. Moon-aware Seestar picks on `seestar.html`. cache -> `scob-sky-v97`. |
| **v3.92** | `solar-system.html`, `sw.js`, `version.js` | **Animation, to tell the story.** The overview page now moves. (1) **Animated retrograde** — a two-panel synced explainer: a top-down view where Earth overtakes the chosen planet with a live orange sight-line whose tip swings back across a "distant stars" ring (the cause), beside the apparent-path loop with a moving marker that traces the backwards loop in step (the effect); with Play / Pause / Restart and a running date read-out that flips to "moving backwards" during the retrograde. It auto-plays on load. (2) **Spinning planets** — the axial-tilt line-up now animates: each planet turns on its real axis (a white surface spot marks the spin) at a speed set by its true rotation period — Jupiter races, Venus barely creeps, and Venus & Uranus turn backwards. All driven by one lightweight timer; still degrades cleanly with no motion if the engine is absent. cache -> `scob-sky-v96`. |
| **v3.91** | `solar-system.html`, `solar-system-planets.html`, `sw.js`, `version.js` | **Side-on views and richer planet detail.** (1) The **overview page** gains a "Seen from the side" card with two new computed diagrams: an **edge-on view of the orbital planes** showing each planet's inclination relative to the ecliptic (tilts exaggerated ×3; the system really is nearly flat, with Pluto's 17° for contrast), and an **axial-tilt & rotation line-up** drawing every planet with its real obliquity, spin axis and equatorial (rotational) plane, spin direction (Venus and Uranus retrograde), and sidereal rotation time. (2) The **planets page** fact cards now include a distinct **Rotation** stat (sidereal spin, separate from the solar day), a dedicated **🌙 Moons** block naming and describing each planet's notable moons (the Galileans, Titan/Enceladus, Triton, Phobos/Deimos, etc.), and **three fun facts per planet** instead of one. cache -> `scob-sky-v95`. |
| **v3.90** | `solar-system-planets.html` (new), `solar-system-outer.html` (new), `solar-system.html`, `scob-dashboard-v3.html`, `about.html`, `sw.js`, `version.js` | **Solar-system feature expanded into a three-page section.** The single page was overcrowded, so it is now a linked trio with a pill navigation across the top of each. (1) **`solar-system.html`** (the hub) keeps the interactive core — the live time-scrub orrery, the apparent-retrograde explorer and the "right now over Jurong" strip — and hands off to the two detail pages. (2) **New `solar-system-planets.html`** — *every planet's orbit on one horizontal scale*: a logarithmic perihelion→aphelion bar chart for all eight planets (plus Ceres and Pluto), so relative spacing, eccentricity and **Pluto's orbit crossing inside Neptune's** are visible at a glance; followed by a detailed **fact card for each of the eight worlds** (distance, orbital period, day length, axial tilt, diameter, mass, gravity, moons, temperature, atmosphere, three key facts, an "at SCOB" viewing note and a fun fact). (3) **New `solar-system-outer.html`** — the outward **log-distance map to the Oort cloud, now marking all eight planets**, plus the tappable-region detail and the encyclopedia panels (asteroid belt, Kuiper belt, dwarf planets, scattered disc & extreme TNOs / **Planet Nine**, heliosphere with **live Voyager 1 & 2 distances**, Oort cloud). Dashboard link relabelled; About-page credit updated to cover all three pages. cache -> `scob-sky-v94`. |
| **v3.89** | `solar-system.html` (new), `scob-dashboard-v3.html`, `about.html`, `sw.js`, `version.js` | **New feature page — the solar system: orbits, retrograde &amp; everything out to the Oort cloud.** A standalone Explore &amp; share page built on the shared engine (`astro-core.js`). (1) **Live top-down orrery** — every planet drawn at its real position for any date, on its true (eccentric) orbit computed on-device; an "Inner (Sun→Mars)" true-scale view and an "All 8" √-compressed view, a ±450-day time slider with Play, an optional Earth→planet sight-line overlay, and a live readout of which planets are retrograde on the chosen date. (2) **Apparent-retrograde explorer** — traces a chosen planet's real geocentric path (ecliptic lon/lat) through its nearest retrograde loop, red-flagging the backwards weeks and dating the stationary points (Mars/Jupiter/Saturn). Windows validated against ephemeris (Mars 2024-12-08→2025-02-24, Jupiter 2025-11→2026-03). (3) **Outward log-distance map** to the Oort cloud — tappable zones: terrestrial planets, asteroid belt (Kirkwood gaps, Trojans), giant planets, Kuiper belt (plutinos 3:2), scattered disc &amp; extreme TNOs (Sedna) with the **Planet Nine** debate (incl. 2026 Rubin search &amp; the "Ammonite" sednoid), heliosphere/heliopause with **live-projected Voyager 1 &amp; 2 distances**, and the Oort cloud. (4) **Region encyclopedia** (planets data table, orbits &amp; Kepler, dwarf planets &amp; candidates, resonances) plus a live "right now over Jurong" strip. Computed on-device, works offline; linked from the ☰ **Explore &amp; share** menu beside "Solar system to scale" and credited on the **About &amp; credits** page (JPL Solar System Dynamics, NASA/JPL Voyager tracking, and the Batygin &amp; Brown Planet Nine hypothesis / Rubin search). cache -> `scob-sky-v93`. |
| **v3.88** | 49 pages, `about.html`, `sw.js`, `version.js` | **Housekeeping — credits reachable from every footer, and the credits page completed.** Before this, the aggregated **About &amp; credits** page was reachable from only one page (itself); now **all 49 content pages carry an "About &amp; credits" footer link** (the 6 instant-redirect stubs are left alone). Completed `about.html` so the credits page is in full parity with the sources cited in individual page footers — added the four that were missing: **qrcode.js** (davidshimjs, via cdnjs; used on the kiosk/object-card/poster pages), the **IAU/USGS Gazetteer of Planetary Nomenclature** (lunar surface features on *Tonight on the Moon*), **meteor-shower data** (International Meteor Organization &amp; American Meteor Society), and the **NASA SDO/HMI** live white-light Sun image. Leanness pass: no orphan pages or broken links (all 12 release-check gates pass); the repo is already fully de-duplicated (shared theme + engine), so no code was removed. Content/plumbing only — no new files. cache -> `scob-sky-v92`. |
| **v3.87** | `cosmic-extremes.html`, `sw.js`, `version.js` | **Completed the nebula taxonomy and filled two gaps on the extreme-objects page.** The Nebulae section listed only three kinds; it now presents the **five main types in two families** — three **diffuse** nebulae (**emission**/H II regions, **reflection**, **dark**) and two marking a **single star's death** (**planetary**, **supernova remnant**) — with Singapore-visible southern examples added: the **Lobster Nebula (NGC 6357)** and Lagoon (emission), the Pleiades haze (reflection) and the naked-eye **Coalsack** by the Southern Cross (dark). Also closed the two clearest omissions elsewhere on the page: **magnetars** in the neutron-star section (strongest magnetic fields known; starquake gamma-ray flares) and **gravitational-wave detection** of merging black holes (LIGO, since 2015) in the stellar-black-hole section. Content-only; no new files or engine changes. cache -> `scob-sky-v91`. |
| **v3.86** | `comets.html` (new), `scob-dashboard-v3.html`, `sky-guide.html`, `about.html`, `sw.js`, `version.js` | **Comet page, dynamic limiting magnitude, seeing surfaced, and the events calendar extended to 2028.** (1) **New `comets.html`** — a dedicated Tonight-style page for the comets the engine already tracks (`astro-core.js` `COMETS` / `cometEq` / `activeComets`): for the chosen date at 8:30 pm it shows each active comet's live magnitude, altitude &amp; compass direction, an altitude-through-the-night curve (with the dark window shaded), rise/culmination/set and best-in-the-dark times, Moon distance, and a scope recommendation; plus a "comets we're tracking" list with each comet's visibility window and element-review date, and a docent script on how to show a diffuse comet. Computed on-device, works offline. Linked from the ☰ **Tonight's session** menu and the public **Sky Guide** (four-language tile). (2) **Dynamic limiting magnitude** — the static Bortle scope-limit is now reduced live for the Moon (illuminated fraction × altitude) and the current haze (west 24-h PSI), shown as a glance figure and feeding the visual deep-sky verdicts ("Below tonight's limit"). (3) **Seeing surfaced** — the existing 7Timer! seeing read now also appears as a glance chip, a run-sheet line, and a note in the Jupiter/Saturn high-power detail. (4) **Events calendar extended through 2028** (eclipses cross-checked against NASA/GSFC; Moon phases computed on-device) with an **expiry guard** that warns on-screen when the hand-maintained list nears its end instead of silently showing nothing. About-page credits updated. cache -> `scob-sky-v90`. |
| **v3.85** | `filters.html` (new), `scob-dashboard-v3.html`, `about.html`, `sw.js`, `version.js` | **New reference page — imaging &amp; visual filters.** A standalone Reference-guides page explaining every astro filter and when to reach for it: the four workhorse **emission lines** (Hα 656.3, OIII 500.7, SII 671.6, Hβ 486.1 nm) on a labelled spectrum; the **mono vs one-shot-colour** fork that decides which filters you buy; **broadband** (UV/IR cut, luminance, light-pollution/CLS, UHC, ND), **narrowband** line filters, **dual/tri/quad-band** filters for OSC and smart scopes, the **SHO/HOO** palettes, **LRGB**, **planetary &amp; lunar** (IR/UV-pass, methane, Wratten, ADC), and **solar** (front white-light, Hα etalon, Ca-K — with the safety warning and a link to `solar.html`), plus a **bandwidth/FWHM** primer, a decision guide and a one-glance cheat sheet. Closes with an **SCOB framing** — ND/polariser for the Moon, Wratten/IR for planets, a front solar filter for daytime, and why the deck **Seestar** leans on a built-in dual-band filter to beat Jurong's light pollution. Linked from the ☰ menu under **Reference guides** (between Eyepiece &amp; FoV and Daytime/solar) and cross-linked to the telescope guide, phone-astrophotography and Seestar pages; credited on the About page. cache -> `scob-sky-v89`. |
| **v3.84** | `scob-dashboard-v3.html`, `moon-tonight.html`, `checklist.html`, `object-cards.html`, `qr-poster.html`, `aboriginal-*`, `inca-*`, `scob-theme.css`, `PUBLISHING.md`, `sw.js`, `version.js` | **Housekeeping closed out — the last pages join the shared theme and the shared engine.** (1) **All 55 pages now load `scob-theme.css`.** `scob-dashboard-v3.html` joins (its two identical `:root` blocks removed), and the three print pages — `checklist`, `object-cards`, `qr-poster` — switch to **`class="paper"`**. Worth recording: the print pages do *not* invert `--navy`/`--navy2`; those stay dark and carry the ink-on-white headings, and only `--gold`, `--cyan`, `--ink`, `--mute` and `--line` change for paper. The first draft of `body.paper` inverted them and would have printed white headings on white. (2) **`--dark` collision resolved** — it meant `#c58b6a` on the Aboriginal pages and `#b98cff` on the Inca ones, one name for two colours. Now `--dark-ochre` and `--dark-violet`, both in the theme. No visual change. (3) **`moon-tonight.html` (5 helpers) and the dashboard (6) move onto `sky-culture-core.js`**, finishing the engine consolidation. The dashboard's `compass` was written as a function with the 16 points inline rather than as an arrow over `CMP`; verified identical to SkyCore's across 1,440 test bearings before swapping. Its helpers live in the eval'd `#sharedScript` block, so the import goes there, not in the trailing `<script>`. (4) **`PUBLISHING.md` brought back in line with the workflow** — it still claimed only html/js/webmanifest/icons are published; `.css` has been staged since v3.82, and the `test-*.js` + `migrate-theme.js` strip is now documented. Verified by an effective-CSS diff against `git HEAD` — every selector, every property, `var()` resolved: **zero differences**. cache -> `scob-sky-v88`. |
| **v3.83** | 51 pages, `sky-culture-core.js`, `scob-theme.css` (new), `migrate-theme.js` (new, dev-only), `check-release.sh`, `.github/workflows/deploy-pages.yml`, `sw.js`, `version.js` | **Two big de-duplications: shared geometry helpers, and a shared stylesheet.** (1) **14 culture pages moved onto `sky-culture-core.js`** — `babylonian`, `chinese`, `egyptian`, `inca`, `indian`, `malay` and `maya`, both `-sky` and `-tonight`. 62 inline helper definitions (`el`, `rev`, `pad`, `compass`, `jd`, `sgtNow`, `nextFridayYMD`) deleted in favour of the shared module, leaving 7 pages on the old pattern → 21 on the new. Page-specific variants were deliberately kept: `chinese-tonight.html` keeps its **language-aware `compass`** (Chinese compass points when 中文 is selected) and `malay-sky.html` keeps its `const compass = document.getElementById(...)`, which merely shares the name. (2) **`scob-theme.css` (new)** replaces 19 near-identical `:root` blocks and ~86 KB of byte-identical rules (`.foot a` on 44 pages, `thead th` / `.btnmini` / `.backlink` / `body.night` on ~20 each) with one stylesheet; **51 pages migrated**, ~53 KB of duplicated CSS removed. Changing the gold or the card radius is now one edit instead of 51. `scob-dashboard-v3.html` and the three print pages are deliberately held back for a later release. Verified by an effective-CSS diff — every selector, every property, `var()` resolved — across all 51 pages: **zero differences**. `--dark` was left page-local: `aboriginal-*` sets it to `#c58b6a` and `inca-*` to `#b98cff`, one name for two colours, which needs a decision rather than a merge. Dev-only `migrate-theme.js` is excluded from the cache and from the published site. cache -> `scob-sky-v87`. |
| **v3.82** | `sky-culture-core.js`, `check-release.sh`, `.github/workflows/deploy-pages.yml`, `sw.js`, `version.js` | **Cross-culture star lookup now spans all ten sky cultures, and the release check maintains itself.** (1) `crossLinks()` in `sky-culture-core.js` previously called only `chineseStarAt` and `nakshatraStarAt`, so eight of the ten loaded cultures never appeared in a cross-culture result — Antares resolved to 心宿二 and Jyeṣṭhā and stopped there. All ten data files share one entry shape, so a single generic RA/Dec scan now serves the rest, driven by a **`CULTURES` registry**: adding a civilisation is one row, no other edit. Chinese and Indian keep their tuned resolvers (Three-Enclosure scan; junction-star flag) and their exact existing return shape, so the Greek, Polynesian and Aboriginal pages that read `L.chinese.mzh` / `L.indian.iast` are untouched. Antares now returns **7** cultures, the Pleiades **8**. New `crossLinkList()` gives a normalised array for renderers, plus `cultures()` / `culturesLoaded()`. (2) **`check-release.sh` no longer hand-maintains its file lists** — `PAGES` (55 entries) and `CACHED` (68) were a copy of `ls` that had to be edited on every release; both are now derived from the folder, and `./check-release.sh --fix` regenerates the `sw.js` ASSETS array. Three new gates: **cache-name lockstep** (fails if `scob-sky-vN` didn't move with the version), **orphan pages** (a page linked from nowhere), and duplicate `*(current)*` README rows. (3) The Pages workflow now stages **`*.css`** as well, and explicitly removes the `test-*.js` dev scripts from the published site. cache -> `scob-sky-v86`. |
| **v3.81** | `space-telescopes.html` (new), `scob-dashboard-v3.html`, `check-release.sh`, `sw.js`, `version.js` | **New reference page — space telescopes, what they see &amp; their orbits.** A standalone Reference-guides page on why telescopes go above the atmosphere (an **atmospheric-windows** diagram of what reaches Jurong vs. what only space can see), what each wavelength band reveals — **radio/microwave, infrared, visible/UV, X-ray, gamma** — organised as a **15-mission fleet** (Hubble, JWST, Chandra, XMM-Newton, Fermi, TESS, Euclid, Roman, Gaia, Kepler, Spitzer, Herschel, WISE, IRAS, Planck), and the **five orbit families** they use (LEO, polar/Sun-sync, high-elliptical, **Sun–Earth L2**, Earth-trailing) with the reasoning behind each — a labelled orbit diagram, a summary table and an SCOB teaching hook (spot Hubble on a pass; the same-object eyepiece-vs-Hubble comparison). Linked from the ☰ menu under **Reference guides**. cache -> `scob-sky-v85`. |
| **v3.80** | `greek-sky-data.js`, `greek-sky.html`, `sw.js`, `version.js` | **Greek page now draws all twelve zodiac constellations.** The Zodiac myth-family previously drew only the 6 brightest signs as star-figures (all 12 were named in the zodiac ring). Added the missing six — **Aries, Cancer, Libra, Capricornus, Aquarius, Pisces** — with their star patterns and myths, so the family is the complete twelve (page total 16 → 22). cache -> `scob-sky-v84`. |
| **v3.79** | `astro-core.js`, `sky-culture-core.js`, `polynesian-tonight.html`, `aboriginal-tonight.html`, `sw.js`, `version.js` | **One star-location engine (consolidation, step 1).** The alt/az geometry that was duplicated across `astro-core.js`, `sky-culture-core.js`, the dashboard and the tonight pages is being unified onto **one source of truth**: added an `altazLST(ra, dec, LST)` primitive to `astro-core.js` (compute the sidereal time once, loop over many stars) and refactored `altazAt` to call it — output verified identical to ~1e-13°. `sky-culture-core.js` now **delegates** its `altAz`/`lstAt` to astro-core instead of carrying its own copy of the formula, so the SCOB location and the maths live in exactly one place. The two star-only tonight pages (Polynesian, Aboriginal) now load astro-core; `sky-culture-core.js` is served network-first so future engine edits propagate. Next steps (later): migrate the dashboard SkyMap and the 7 older tonight pages to the same primitive. cache -> `scob-sky-v83`. |
| **v3.78** | `sky-culture-core.js` (new), `polynesian-sky.html`/`-tonight.html`/`-sky-data.js` (new), `aboriginal-sky.html`/`-tonight.html`/`-sky-data.js` (new), `greek-sky.html`/`-tonight.html`/`-sky-data.js` (new), `scob-dashboard-v3.html`, `sky-guide.html`, `about.html`, `world-skies-roadmap.md`, `check-release.sh`, `sw.js`, `version.js` | **The final three cultures + a shared engine.** **Polynesian:** the four Hawaiian wayfinding **star lines** (Ke Ka o Makaliʻi, Ka Iwikuamoʻo, Manaiakalani, Ka Lupe o Kawelo), the star compass, and marks like Hōkūleʻa (Arcturus) and Māui's fishhook (Scorpius); the tonight page shows which lines are up and how to find south. **Aboriginal Australian:** the **Emu in the Sky** (a dark constellation reusing the Inca dark-cloud renderer), the Seven Sisters (Pleiades), the canoe of Orion and the Southern Cross, with the Emu's seasonal calendar. **Greek:** the classical constellations by myth family (the Perseus legend, the zodiac, Orion & the hunt, gods & heroes) with the 12 zodiac and rich cross-links, plus the Sun's sign on the tonight page. **Engine consolidation:** the star-location maths, time helpers, SVG helper, compass and cross-culture links now live in one shared **`sky-culture-core.js`** — the three new cultures are built on it (leaner pages); the earlier pages keep their inline copies and can be migrated to it page-by-page. **This completes the world sky-cultures set (10).** cache -> `scob-sky-v82`. |
| **v3.77** | `maya-sky.html` (new), `maya-tonight.html` (new), `maya-sky-data.js` (new), `inca-sky.html` (new), `inca-tonight.html` (new), `inca-sky-data.js` (new), `scob-dashboard-v3.html`, `sky-guide.html`, `about.html`, `check-release.sh`, `sw.js`, `version.js` | **Two New-World cultures: the Maya &amp; Inca skies.** **Maya:** Tzab (the rattlesnake's rattle, the Pleiades), the Turtle &amp; Three Hearthstones (Orion, with M42 as the creation fire), the Scorpion, the Peccaries (Gemini) and Xaman Ek' (Polaris), grouped as hearth / sky-road / pivot — plus **Chak Ek' (Venus)** and the 8-year Venus pentagram at the heart of the Maya calendars (`maya-sky.html`); the tonight page tracks Venus as morning/evening star. **Inca:** the first culture here with **dark-cloud constellations** — animals read in the black dust-lanes of **Mayu** (the Milky Way): **Yacana** the Llama (eyes α &amp; β Centauri), Yutu, the Fox, Toad and Serpent — alongside the bright **Qullqa** (Pleiades, the Andean harvest star) and **Chakana** (the Southern Cross) (`inca-sky.html`). A new renderer draws the dark clouds as shaded silhouettes over a traced Milky Way band, on the maps and the live dome. Both cross-link shared stars to their Chinese mansion &amp; Indian nakshatra (dark clouds honestly show *none*), and both join the **Skies across cultures** menu + Sky Guide. Release housekeeping: About credits, `sw.js` cache, `check-release.sh`. cache -> `scob-sky-v81`. |
| **v3.76** | `babylonian-sky.html` (new), `babylonian-tonight.html` (new), `babylonian-sky-data.js` (new), `egyptian-sky.html` (new), `egyptian-tonight.html` (new), `egyptian-sky-data.js` (new), `scob-dashboard-v3.html`, `sky-guide.html`, `about.html`, `check-release.sh`, `sw.js`, `version.js` | **Two ancient cultures: the Babylonian &amp; Egyptian skies.** **Babylonian:** the constellations of **MUL.APIN** sorted into the **Three Paths** of Enlil (north), Anu (the equatorial road) and Ea (south) — 18 figures that are the ancestors of the Western constellations — plus the **12-sign zodiac Babylon invented** (`babylonian-sky.html`); the tonight page shows the Three Paths and the sign the Sun stands in. **Egyptian:** **Sah** (Orion/Osiris), **Sopdet** (Sirius/Isis), **Meskhetiu** (the Big Dipper) and **Taweret** (Draco), split into the **Imperishable** (circumpolar) and **Unwearying** (rising/setting) stars, with the **36 decans** that gave us the 24-hour day and the 365-day year (`egyptian-sky.html`); the tonight page tracks Sopdet. Both pages cross-link every shared star to its Chinese mansion and Indian nakshatra, and both join the **Skies across cultures** menu + multilingual Sky Guide. Release housekeeping: About credits, `sw.js` cache, `check-release.sh` all updated. cache -> `scob-sky-v80`. |
| **v3.75** | `malay-sky.html` (new), `malay-tonight.html` (new), `malay-sky-data.js` (new), `nakshatra-sky-data.js`, `scob-dashboard-v3.html`, `sky-guide.html`, `about.html`, `test-logic.js`, `check-release.sh`, `sw.js`, `version.js` | **The Malay / Nusantara sky + release-chain housekeeping.** Added a third culture: **`malay-sky.html`** (a star-compass *mata angin*, the indigenous star-pictures — Bintang Pari/Crux, Belantik-Waluku/Orion, Biduk/Big Dipper, Bintang Tujuh-Kartika/Pleiades, Kala Jengking/Scorpius — with a Western overlay, a per-figure deep dive that cross-links **both** the Chinese mansion and Indian nakshatra of every shared star, and a **12-buruj** zodiac framework in Rumi + Jawi) and **`malay-tonight.html`** (a live "star compass now", the Sun's buruj, and Venus as Bintang Timur/Kejora). Added `nakshatraStarAt()` to the nakshatra data for cross-culture lookups. **Housekeeping:** refreshed the **About & credits** page for all three cultures and their data sources; added a **`test-logic.js` guard** that fails the build if any culture page isn't credited/linked in `about.html`; brought `sw.js` (offline cache) and `check-release.sh` up to date with every new page and data file. cache -> `scob-sky-v79`. |
| **v3.74** | `indian-sky.html` (new), `indian-tonight.html` (new), `nakshatra-sky-data.js` (new), `scob-dashboard-v3.html`, `sky-guide.html` | **The Indian sky — 27 nakshatras.** A new culture pair mirroring the Chinese pages, English-first with Devanagari + IAST names: **`indian-sky.html`** (a nine-planetary-lord wheel, strip/circle sky maps with a Western overlay, a per-nakshatra deep dive with deity/symbol/lord/gaṇa and a Chinese-mansion cross-link, and a 12-rāśi zodiac framework explaining sidereal vs tropical / ayanāṁśa) and **`indian-tonight.html`** (the Moon's nakshatra with pada & lord, and a full on-device pañchāṅga — vāra, tithi, nakshatra, yoga, karaṇa). Introduced the new **"Skies across cultures"** menu section on the dashboard to group the Chinese, Indian (and later Malay) pages, and added tiles to the multilingual Sky Guide. Junction stars after the standard yogatārā list, cross-checked to Hipparcos J2000; sidereal work uses the Lahiri ayanāṁśa. cache -> `scob-sky-v78`. |
| **v3.73** | `chinese-tonight.html`, `chinese-sky.html`, `version.js`, `sw.js` | **Recoloured the Three Enclosures to stand apart from the 28 mansions.** On the tonight dome the enclosure blue/teal sat too close to the mansions' tortoise-blue and dragon-green. The enclosures now use a distinct **violet / magenta / amber** trio (紫微 violet, 太微 magenta, 天市 amber) — well outside the mansions' green/blue/white/coral — so the ◆ enclosure landmarks read clearly against the ● mansion dots. Applied consistently across the pole-map, chips and dome. cache -> `scob-sky-v77`. |
| **v3.72** | `chinese-tonight.html`, `version.js`, `sw.js` | **Enclosures on the tonight sky-map + tap-to-locate.** The Three-Enclosure landmarks now plot on the all-sky dome as **◆ diamonds** (coloured by enclosure), alongside the ● mansion dots — so 北斗, 織女, 河鼓, 大角 etc. appear in their real place in the sky, not just in the list. And, following the dashboard's pattern, **tapping any mansion, enclosure star, dome marker or table row pins it with a pulsing gold ring** on the chart and a note giving its altitude and compass direction (or "below the horizon now"); tap again to clear. Works in both languages and updates live as you scrub the time. cache -> `scob-sky-v76`. |
| **v3.71** | `chinese-tonight.html`, `scob-dashboard-v3.html`, `sky-data.js` (new), `sw.js`, `.gitattributes`, `version.js` | **Two improvements + housekeeping.** (1) **The Three Enclosures now show live on the "Chinese sky tonight" page** — a new card lists the sky's great signposts (北斗 Big Dipper, 織女 Vega, 河鼓 Altair, 大角 Arcturus, 帝座 Rasalgethi, 五帝座 Denebola, 北極 Kochab), grouped by enclosure with each one's current altitude and compass direction (or "below the horizon"), computed on-device from the shared `CENC` data. (2) **Slimmed the dashboard**: the deep-sky/double-star catalogue (`CAT`, 229 rows) and constellation stick-figures (`CONST`, 16) moved out of `scob-dashboard-v3.html` into a new shared **`sky-data.js`** (loaded before the main script) — the dashboard HTML drops from 189 KB to 170 KB and the data can now be reused by other pages. **Housekeeping:** fixed the recurring **LICENSE CRLF churn** for good — `.gitattributes` now uses `text eol=lf` (the old `-text` marked it binary so Git compared raw bytes and flagged it modified every checkout). cache -> `scob-sky-v75`. |
| **v3.70** | `scob-dashboard-v3.html`, `version.js`, `sw.js` | **Tuned the header-collapse animation to be perfectly smooth.** The v3.69 version animated `max-height` from a fixed 360px, so on collapse nothing moved for the first ~100 ms (until the cap reached the real content height) and then it dropped — a slight hitch. Now JS measures each group's exact height and animates from *that* to 0 (and releases the cap to `none` when open, so the glance/weather filling in never clips). Eased with `cubic-bezier(.4,0,.2,1)` over 0.3 s and a wider hysteresis band (collapse >72px, expand <20px) for a clean, jump-free slide. |
| **v3.69** | `scob-dashboard-v3.html`, `version.js`, `sw.js` | **Smoothed the desktop header collapse.** v3.68 hid the header sections with `display:none`, which snapped shut in one frame and jumped the page content (a flicker). The collapsible parts are now wrapped in two groups that **animate their height and fade** over ~0.24 s, so the header slides open/closed smoothly like the mobile bar instead of popping. |
| **v3.68** | `scob-dashboard-v3.html`, `version.js`, `sw.js` | **Desktop dashboard header now collapses on scroll.** The sticky top bar (title, control row, day-note, glance status and dark-bar) took up nearly half the screen once you scrolled. It still shows in full on the initial view, but as soon as you scroll it **minimises to just the date, time and the time-scrubber** (plus ● Now and the countdown), handing the sky map and cards back most of the viewport; it restores when you scroll back to the top. Scoped to desktop (mobile already uses the slim bar), with hysteresis to avoid flicker at the fold. cache -> `scob-sky-v72`. |
| **v3.67** | `chinese-sky.html`, `version.js`, `sw.js` | **Enclosure map labels de-crowded.** On the Three-Enclosures pole-map the asterism names were bunched on top of each other. Now only the **selected** enclosure is labelled (the other two stay as faint background figures), and each name is offset outward with a short **leader line** back to the star/asterism it names, with a vertical de-collision pass and edge-aware text anchoring so names fan out cleanly and never run off the map — while still pointing at the right star. cache -> `scob-sky-v71`. |
| **v3.66** | `chinese-sky-data.js`, `chinese-sky.html`, `version.js`, `sw.js` | **The Three Enclosures 三垣 added to the Chinese Sky page.** Beyond the 28 ecliptic mansions, the three great circumpolar/zodiacal "walled cities" — **紫微垣** Purple Forbidden (the pole, Draco &amp; the Big Dipper), **太微垣** Supreme Palace (Leo–Virgo) and **天市垣** Heavenly Market (Hercules–Ophiuchus) — now complete the educational story. A new **north-pole star-map** (the wow feature) plots all three enclosures with their wall asterisms plus the great markers 北斗 Big Dipper, 織女 Vega, 河鼓 Altair, 大角 Arcturus and 帝座 Rasalgethi; tap an enclosure to light it up and read its story, with the **same Chinese ↔ Western star names** as the mansions (天樞 = Dubhe, 帝 = Kochab, 右執法 = β Vir…). 14 asterisms, 62 named stars, from Stellarium's Chinese sky culture (HIP + line links) coordinate-matched to the **Hong Kong Space Museum** bilingual list (via Wikipedia); every star position machine-verified against known catalogue coordinates. The `chineseStarAt`/`chineseStarByName` cross-links now also resolve enclosure stars, so the tools recognise Vega → 織女一, Arcturus → 大角, the Dipper stars, etc. cache -> `scob-sky-v70`. |
| **v3.65** | `chinese-sky-data.js`, `tonights-tour.html`, `kiosk.html`, `scob-dashboard-v3.html`, `version.js`, `sw.js` | **Chinese star names cross-linked into the tools.** Two small lookups added to `chinese-sky-data.js` — `chineseStarAt(ra,dec)` and `chineseStarByName(name)` — return a star's traditional Chinese mansion-name (e.g. Antares → 心宿二, Heart mansion) if it's one of the 28-mansion members. **Tonight's Tour** and the **visitor kiosk** now show a 🐉 mansion chip on each showpiece star (kiosk in the current language); the **dashboard sky-map tooltip** appends the mansion name when you tap a bright star. Verified against the roster: Antares 心宿二, Spica 角宿一, Rigel 參宿七, Betelgeuse 參宿四, Aldebaran 畢宿五, Shaula 尾宿八 — and stars outside the 28 mansions (Sirius, Vega, Regulus) correctly show nothing. cache -> `scob-sky-v69`. |
| **v3.64** | `chinese-sky.html`, `version.js`, `sw.js` | **Combined map now shows full figures, not just dots.** The Western overlay looked sparse because the mansions were drawn as single determinative dots while the constellations were full stick-figures — so they didn't visibly "line up." Now, with the overlay on, the **full 28-mansion asterisms** (member stars + line links, coloured by symbol) are drawn alongside the Greek figures, so you can see the same stars belong to both — e.g. Scorpius *is* 房 + 心 + 尾, Orion *is* 參 + 觜. Verified coordinate-correct: every star shared by both systems (Antares = 心宿二, Spica = 角宿一, Aldebaran = 畢宿五…) projects to the identical spot. Added an explanatory caption noting the mansions form an ecliptic belt, so far-north/south constellations (Ursa Major, Crux…) correctly have no mansion. cache -> `scob-sky-v68`. |
| **v3.63** | `chinese-sky.html`, `version.js`, `sw.js` | **Fixes for the combined-map release (v3.62).** Two bugs: the **Overlay Western constellations** toggle blanked the strip because its helper called `rev()`, which wasn't defined on this page (now defined) — the overlay now actually draws. And the **circle chart never hid** in strip view: the `#circle` CSS rule carried `display:block`, which (as an ID selector) overrode `.mapview{display:none}`, so both charts showed at once; removed it so the strip/circle toggle is exclusive again. Also widened the overlay band to ±64° so Crux fits. cache -> `scob-sky-v67`. |
| **v3.62** | `chinese-sky-data.js`, `chinese-sky.html`, `version.js`, `sw.js` | **Combined Western + Chinese sky map.** The Chinese Sky page's strip map gains a **🌌 Overlay Western constellations** toggle: the Greek stick-figures (16 constellations — Orion, Scorpius, Taurus, Sagittarius…) are drawn over the same RA×Dec chart as the 28 mansions, so you can see both traditions on one sky at once (the chart widens its declination band to ±62° to fit the whole-sky figures). The constellation geometry is shared from `constellation-story.html` into `chinese-sky-data.js` as `window.WCONST`. A genuine cross-cultural view — the same stars, named two ways. cache -> `scob-sky-v66`. |
| **v3.61** | `sky-guide.html`, `scob-dashboard-v3.html`, `chinese-sky.html`, `chinese-tonight.html`, `.gitattributes` (new), `publish.bat`, `version.js`, `sw.js` | **Reach, integration &amp; polish for the Chinese-sky feature.** The public **Sky Guide** now carries two four-language tiles — *The Chinese sky* (28 mansions &amp; Four Symbols) and *The Chinese sky tonight* — so visitors reach them, not just volunteers via the ☰ menu. The **dashboard glance card** gains a **🐉 Moon-lodge chip** ("Moon lodges in 危宿 Rooftop"), computing which of the 28 mansions the Moon sits in from its RA (loads `chinese-sky-data.js`; precessed to date). **Accessibility** on both Chinese pages: the view toggles, animal cards and mansion chips are now keyboard-operable (Tab + Enter/Space, `role="button"`) and the night/language icon-buttons have aria-labels. **Housekeeping:** a new `.gitattributes` stops the `LICENSE` CR/LF churn, and `publish.bat` now clears a stale `.git\index.lock` before committing (ending that recurring error). cache -> `scob-sky-v65`. |
| **v3.60** | `chinese-sky-data.js`, `chinese-sky.html`, `about.html`, `version.js`, `sw.js` | **Authoritative Chinese ↔ Western star names.** Each mansion's deep-dive now shows its full traditional roster with the **Chinese star name paired to the Western name** — 心宿一 = σ Sco (Alniyat), 心宿二 = Antares (α Sco), 心宿三 = τ Sco, etc. — sourced from the **Hong Kong Space Museum** bilingual list (via Wikipedia's *List of Chinese star names*; after Yi Shitong 1981 and Chen Meidong 1996), pulled for all 28 mansions. The chart plots the Chinese 宿-name (中文 mode) or the Western designation (English mode) beside each star; the table lists the complete numeral-ordered roster with the marker (距星) flagged. This replaced the earlier coordinate-guessed labels — every star was re-identified by matching its Hipparcos number and coordinates to the authoritative roster, which also **corrected real mislabels** (e.g. the Ghost's γ/δ Cnc had been swapped; the Beak's φ¹/φ² Ori collided; several determinatives were relinked to their true 宿一 reference star). Credits updated on the page footer and the About page. cache -> `scob-sky-v64`. |
| **v3.59** | `chinese-sky-data.js`, `chinese-sky.html`, `version.js`, `sw.js` | **Real star names in the deep-dive.** Each mansion's full asterism now labels every member star with its actual Western name instead of &ldquo;star 2 / star 3&rdquo;: **86** stars carry their IAU proper name and/or Bayer designation (Antares α Sco, Betelgeuse α Ori, Shaula λ Sco, Sargas θ Sco…), and the remaining **48** fainter members show their exact **Hipparcos catalogue number** — obtained by matching each star's J2000 position to the Hipparcos catalogue, so every label is correct rather than a guessed Greek letter. Names are plotted beside the stars on the asterism chart and listed in the star table. This pass also corrected several **determinative (距星) markers** that approximate seed coordinates had drifted onto the wrong member — the Heart now marks its true reference σ Sco (Alniyat) rather than Antares, the Dipper marks φ Sgr rather than Nunki — and every marker is now labelled with its own real star name. (Per-star *Chinese* names were left out: the authoritative catalogue numbering isn't cleanly derivable and shouldn't be guessed.) cache -> `scob-sky-v63`. |
| **v3.58** | `about.html`, `version.js`, `sw.js` | **Credit for the almanac cycle + README reordered.** The *About &amp; Credits* page now lists the almanac &ldquo;mansion of the day&rdquo; under "Where the numbers come from" — the traditional East-Asian koyomi 二十八宿 (a continuous 28-day calendar cycle, distinct from the Moon's real position), cross-checked against the calc-site and koyominote almanacs and the National Astronomical Observatory of Japan koyomi documentation. This **version history is now ordered newest-first** (it had grown long to scroll to the bottom). Housekeeping: expanded `check-release.sh` to also verify the two Chinese-sky pages and `chinese-sky-data.js` (now 81 checks); confirmed no stray/temp files, no debug leftovers, coordinates uniform, and the six redirect stubs intact. cache -> `scob-sky-v62`. |
| **v3.57** | `chinese-tonight.html`, `version.js`, `sw.js` | **Almanac mansion alongside the astronomical one.** The tonight page now also shows the **traditional calendar mansion** (📅) for the date — the fixed 28-day-cycle "mansion of the day" used in Japanese/Chinese almanacs for date-selection and fortune — clearly labelled and distinguished from the 🌙 **astronomical** lodge (where the Moon actually is among the stars). The calendar cycle (continuous mod-28 day count, offset 7 on days-since-epoch, traditional 角→軫 order) was cross-verified against **three independent sources**: it reproduces the calc-site.com almanac and the koyominote.com almanac exactly for all of July 2026 (31/31 each), and matches calc-site again for January 2000 (26 years away, confirming no resets) — consistent with the National Astronomical Observatory of Japan's documentation that the mansions have been assigned to days *continuously* since the Jōkyō calendar (1685) as a day-count distinct from the Moon's real position. A footer note explains that the two systems answer different questions and rarely agree, because the almanac cycle drifted from the real sky centuries ago. cache -> `scob-sky-v61`. |
| **v3.56** | `chinese-tonight.html`, `version.js`, `sw.js` | **Time slider on the Chinese-sky-tonight page.** Added a gold **drag slider** (same 0–1439-minute, 5-min-step control as the main dashboard) so you can scrub through the whole night and watch the mansions — and the Moon and its lodge — rise, culminate and set, without re-typing the time. The slider stays in sync with the date/time pickers and the ↺ Next Fri / ● Now buttons (Now still live-updates each minute). cache -> `scob-sky-v60`. |
| **v3.55** | `about.html`, `chinese-tonight.html`, `version.js`, `sw.js` | **Credits + a Moon's-lodge accuracy fix.** The *About &amp; Credits* page now attributes the new star data: under "Where the numbers come from", a line crediting the Twenty-Eight Mansions member stars and asterism line-links to the **Stellarium "Chinese" sky culture** joined to **Hipparcos (ESA) J2000** positions; and under "Sky lore", the Chinese entry links to the two new pages and cites **Sun Xiaochun &amp; Jacob Kistemaker, _The Chinese Sky During the Han_ (Brill, 1997)**. Also fixed the **Moon's-lodge frame mismatch**: `astro-core` gives the Moon's position in the equator *of date*, but the marker-star boundaries were J2000 — a ~0.35° offset that flipped the assigned lodge on ~6% of near-boundary nights. The boundaries are now **precessed to the date** (Newcomb) before comparison, so Moon and stars are in the same frame; verified 48/48 nights across 2026 fall inside their assigned mansion's window (which tile the sky to exactly 360°). The tonight sky-map now also **draws the Moon itself** (a ◑ marker at its true altitude/azimuth) and **rings its lodge mansion in gold**, and the lodge card states plainly whether the Moon is up (with direction) or still below the horizon — fixing the confusion where the lodge was named but nothing showed on the chart (either because the Moon hadn't risen, or because the mansion's marker star sits at its western edge, below the horizon, while the Moon is further east and already up). cache -> `scob-sky-v59`. |
| **v3.54** | `chinese-sky-data.js` (new), `chinese-sky.html`, `chinese-tonight.html`, `version.js`, `sw.js` | **Chinese-sky pages: full asterisms, a 中文 toggle, and the Moon's lodge.** A new shared **`chinese-sky-data.js`** holds all 28 mansions with their **full member-star asterisms** (134 stars to ~mag 5, Hipparcos J2000 positions joined to the Stellarium Chinese-sky-culture line links), so the reference page's deep-dive now draws **every mansion's complete star official** — pick any of the 28 and see its shape over the real stars — instead of only three. Both pages gain an **English ⇄ 中文 toggle** (remembered per visitor): the essays, UI, mansion meanings, captions and compass all switch language, while the 漢字 star names always show. The tonight page adds the **Moon's lodge** — using the shared `astro-core.js` engine it computes which mansion the Moon rests in tonight ("🌙 Tonight the Moon lodges in 房 Room"), with its illumination and altitude; validated to advance exactly one lodge per night as the lunar-mansion system intends. Fainter members (below Jurong's ~mag-5 city-sky limit) are omitted, noted in the footer. cache -> `scob-sky-v58`. |
| **v3.53** | `chinese-sky.html`, `chinese-tonight.html`, `version.js`, `sw.js` | **Housekeeping — no-translate on the two Chinese-sky pages.** Both pages carry a lot of 中文 (mansion names, the Four Symbols), so — matching the kiosk and Sky Guide convention — they now declare `translate="no"` on `<html>` plus a `notranslate` meta tag, so Chrome no longer offers to auto-translate the Chinese star names into English (which defeats the purpose of a page teaching those names). Full audit run alongside: all four suites pass, both pages tag-balanced, coordinates uniform (1.3342°N, 103.7357°E on the tonight page; the reference page computes nothing), every `<button>` has `type="button"`, every `<svg>` has `role="img"`, no debug leftovers, links resolve both ways. cache -> `scob-sky-v57`. |
| **v3.52** | `chinese-sky.html` (new), `chinese-tonight.html` (new), `scob-dashboard-v3.html`, `version.js`, `sw.js` | **Two new pages — the Chinese sky.** **`chinese-sky.html`** cross-references the traditional Chinese sky with the modern Western one: the **Four Symbols** (Azure Dragon, Black Tortoise, White Tiger, Vermilion Bird) and the **Twenty-Eight Mansions** (二十八宿), with a short "why 28 / why four animals" explainer. The 28 mansions appear in two switchable sky-map views — a wide **strip map** (default on desktop, horizontally scrollable on phones) and a **traditional circular chart** (default on phones — pole at centre, declination outward, the four palaces holding the four quarters) — plus a full **Chinese-mansion ↔ Western-star table** (each mansion by its determinative star) and a **Scorpius deep-dive** showing every star of the Room/Heart/Tail mansions. Tapping any animal filters the wheel, map and table together; all interactions are tap-friendly. **`chinese-tonight.html`** is the live "what's up tonight" companion — it computes on-device (for SCOB, at the session time, date/time adjustable) **which of the 28 mansions are above the horizon**, with a plain-language glance ("the Azure Dragon rides high, climbing out of the east"), per-symbol up-count chips, an **all-sky chart** (looking up, N top/E left) of the mansions currently up, and a ranked list with each mansion's altitude, compass direction and rising/setting state. Kept as a separate page so the reference stands alone for any visitor, of any background. Both are mobile-first and linked from the dashboard ☰ menu (Explore &amp; share) and each other. cache -> `scob-sky-v56`. |
| **v3.51** | `meteors.html`, `version.js`, `sw.js` | **Housekeeping.** Full audit — all suites pass, coordinates uniform, no orphan pages, no debug leftovers, every stub link resolves. Found and fixed one real issue: the “Why after midnight” card on `meteors.html` had a malformed `.wow` box (a stray `</p>` leaving the card `<div>` unclosed) — corrected, and a duplicated `td .nm` CSS rule merged. No visible change; all 34 pages now tag-balanced. cache -> `scob-sky-v55`. |
| **v3.50** | `meteors.html`, `version.js`, `sw.js` | **Meteor table improved.** The shower table is now **ordered by date — soonest peak at the top** (a live countdown, “in N days”), instead of by radiant height. And it now shows **which way to look**: the radiant's **compass direction and height at its best in the pre-dawn hours** (e.g. “S · 73°”), which is both more useful and more honest than the theoretical transit height — several radiants only reach their maximum in daylight. The rating and the live hero use the same pre-dawn figure. cache -> `scob-sky-v54`. |
| **v3.49** | `meteors.html`, `version.js`, `sw.js` | **Fix:** in the meteor-shower ranking table, the shower name and its “peaks …” detail line were running together (the detail span was inline) — the detail now sits on its own line beneath the name. cache -> `scob-sky-v53`. |
| **v3.48** | `meteors.html` (new), `scob-dashboard-v3.html`, `almanac.html`, `sky-guide.html`, `version.js`, `sw.js` | **New meteor-shower explainer.** `meteors.html` explains what a shower is (a comet’s debris trail Earth crosses on the same dates each year), why the **radiant** is a perspective effect, why the **after-midnight** hours win, and Singapore’s twist — rate falls with the sine of the radiant’s altitude, so the famous **Perseids/Quadrantids/Ursids underperform** from our latitude while the equator-friendly **Eta Aquariids and Orionids (both Halley’s Comet) are excellent**. Includes a **live “next shower” panel** (name, days to peak, ZHR, radiant height from SCOB, this-year’s Moon interference — all computed on-device) and a table ranking the year’s nine showers for Singapore, plus naked-eye viewing tips. Linked from the dashboard menu, the almanac’s shower card, and the Sky Guide (4-lang tile). cache -> `scob-sky-v52`. |
| **v3.47** | `about.html` (new), `scob-dashboard-v3.html`, `version.js`, `sw.js` | **New About &amp; Credits page.** `about.html` consolidates what had been scattered across page footers into one authoritative page: what the app is (offline-first, on-device), a **privacy note** (no accounts, no tracking; only the live weather leaves the device), a full **data-sources list** (Schlyter, Meeus, AstroPixels/Espenak, WDS/SIMBAD, CelesTrak+satellite.js, Open-Meteo, MSS, NEA, NOAA SWPC, Sea &amp; Sky, NASA/ESA-EHT, ZWO — each linked), respectful **sourcing for the cultural sky-lore** (Chinese 28-mansion system, Malay/Javanese lore, Indian nakshatra tradition with Tamil names, with an invitation to send corrections), and the telescope fleet. Linked from the dashboard menu (Reference guides). cache -> `scob-sky-v51`. |
| **v3.46** | `constellation-story.html`, `cosmic-extremes.html`, `singapore-sky.html`, `light-pollution.html`, `version.js`, `sw.js` | **Sky lore finished, Tamil added, learn-more links.** The “same stars, around the world” section now covers **all 16 constellations** (was 6) — including the Chinese *Qixi* love story spanning Vega (the Weaving Girl 織女), Altair (the Cowherd 牛郎) and Cygnus (the Celestial Ford 天津) across the Milky Way; Alpha Centauri as the Chinese *Southern Gate* 南門; Arcturus as the *Great Horn* 大角; and more. The Indian entries now carry their **Tamil star-names** too (Krittika/Kārttikai, Jyeshtha/Kēṭṭai, Chitra/Chittirai, Magha/Magam…), matching Singapore’s Tamil community. All names web-verified against the Chinese lunar-mansion, Malay/Javanese and Indian nakshatra traditions. Also added **“Learn more” external links** (NASA, ESA/ESO, DarkSky) to the cosmic-extremes, Singapore-sky and light-pollution explainers. cache -> `scob-sky-v50`. |
| **v3.45** | `constellation-story.html`, `version.js`, `sw.js` | **“The same stars, around the world” — multicultural sky lore.** The constellation page now adds, for six of the most recognisable patterns, how **Singapore’s own Chinese, Malay and Indian communities** named and pictured the very same stars — alongside the Greek myth already there. Orion’s belt as the Chinese *Shēn* (參), the Malay *Bintang Tiga Beradik* used to find the qibla, and the Indian *Mrigashira*; the Pleiades as *Mǎo* / *Bintang Tujuh* / *Krittika*; the Big Dipper as *Běidǒu* / *Buruj Biduk* / the *Saptarishi* Seven Sages; Scorpius’s heart *Xīn* in the Azure Dragon; the Southern Cross as the Malay stingray *Bintang Pari* and the Javanese *Gubug Penceng*; and Sirius as the Chinese Celestial Wolf *Tiānláng*. Names verified against the Chinese lunar-mansion, Malay/Javanese and Indian nakshatra traditions, with an honest note that cultures grouped the stars differently. cache -> `scob-sky-v49`. |
| **v3.44** | `version.js`, `sw.js` | **Footer copyright domain corrected** to **`AFBOOSTER.online`** (the v3.43 `.app` was a typo). Single source — `SCOB_COPYRIGHT` in `version.js` — so the footer updates on every page at once. cache -> `scob-sky-v48`. |
| **v3.43** | `version.js`, `sw.js` | **Footer copyright domain changed** from `AFBOOSTER.cc` to **`AFBOOSTER.app`**. It lives in one place — `SCOB_COPYRIGHT` in `version.js` — so the footer updates across every page at once. cache -> `scob-sky-v47`. |
| **v3.42** | all pages (attribute tidy), `sw.js`, `check-release.sh`, `version.js` | **House-keeping pass — verify &amp; tighten.** Audited every page: all four test suites pass, no debug leftovers, no insecure links, no hard-coded versions, coordinates uniform (1.3342°N, 103.7357°E), every page carries charset/viewport/lang and the version plumbing, all tags balanced, all six redirect stubs consistent, every companion page links home. Tightened three things: added explicit **`type="button"`** to all 69 interactive buttons (prevents any future form-submit footgun; behaviour unchanged), gave the static diagram SVGs (eyepiece field, Moon disc, finder compass dial) **`role="img"` + aria-labels** for screen readers, and added the last two redirect stubs (`main.html`, `scob-dashboard.html`) to the offline cache so every entry point works offline like the others. No functional/visual change. cache -> `scob-sky-v46`. |
| **v3.41** | `scale-model.html`, `test-logic.js`, `version.js`, `sw.js` | **Solar-system-to-scale gains a "where does it really end?" section.** Below the planets it now shows the solar system&rsquo;s **three** edges — the planets (Neptune), the Sun&rsquo;s wind bubble (the heliopause, ~120 AU, the only boundary the two Voyagers have crossed), and the Sun&rsquo;s gravity (the outer Oort Cloud, ~100,000 AU / ~1.6 ly, ~a third of the way to the next star) — each honest that the Oort Cloud has never been seen and is inferred from comets. The payoff at the 3 m SCOB Sun: Neptune is 10 km away but the Sun&rsquo;s comet cloud reaches four-fifths of the way around the Earth, and Proxima only 2.2× around. Crucially, big model distances are now written as **everyday journeys** — &lsquo;four-fifths of the way around the Earth&rsquo;, &lsquo;Singapore to London&rsquo;, &lsquo;the distance to the Moon&rsquo; — instead of raw tens-of-thousands of km, and they rescale with the chosen Sun size. New `humanRef()` formatter, guarded by test-logic.js. cache -> `scob-sky-v45`. |
| **v3.40** | `sw.js`, `version.js` | **Fix: the version footer (and the astronomy engine) could get pinned to an old build by the service-worker cache.** The service worker served pages network-first but `version.js` and `astro-core.js` **cache-first**, so after uploading a new build the footer kept showing the previously-cached version until the whole cache happened to rebuild. Both files are now **network-first** (fresh when online, cache only as the offline fallback), so an updated site shows its true version immediately. Existing installs pick this up after the service worker updates once (see the release notes / unstick steps). cache -> `scob-sky-v44`. |
| **v3.39** | `cosmic-extremes.html` (new), `scob-dashboard-v3.html`, `sky-guide.html`, `version.js`, `sw.js` | **New visitor page — the universe at its most extreme.** `cosmic-extremes.html` introduces the exotic objects visitors always ask about — **nebulae** (emission star-nurseries, planetary nebulae, supernova remnants), **neutron stars & pulsars** (1.5 solar masses in a 20 km ball; the Crab pulsar spinning 30×/second), **stellar black holes** (Cygnus X-1, and the honest debunk of the 'cosmic vacuum cleaner' myth), and **supermassive black holes** (our own Sgr A* at 4 million Suns; M87*, the first ever photographed, 2019). The SCOB hook: each class links to a real, *pointable* host object — the Orion & Crab nebulae, the Cygnus X-1 field, the Milky Way core toward Sgr A* — with a live 'point at one right now' hero and per-section altitude, plus an honest note on what you'll actually see (usually a faint smudge hiding something extraordinary). Computed on-device; figures are rounded reference values credited to NASA / ESA-EHT. Linked from the dashboard menu and the Sky Guide (four-language tile). cache -> `scob-sky-v43`. |
| **v3.38** | `sky-events.html` (new), `occultations.html`→stub, `iss-transits.html`→stub, `scob-dashboard-v3.html`, `almanac.html`, `test-pages.js`, `version.js`, `sw.js` | **Consolidation: the two rare-event predictors merged into one tabbed page.** New **`sky-events.html`** holds both tools behind a tab switch — **Occultations & close passes** (the Moon covering or grazing a bright star/planet, topocentric) and **ISS & Tiangong transits** (the station crossing the Sun or Moon). Same on-device computation as before, now one destination instead of two; `?tab=transits` (and `#transits`) opens the second tab directly. `occultations.html` and `iss-transits.html` are now redirect stubs (the almanac's old occultations link and any printed references keep working). The dashboard menu and the almanac now link one **Sky events** entry. Assembled by namespacing the transits event-card CSS (`.ev`→`.tev`) and isolating each tool's script in its own closure, so nothing collides. Also hardened the smoke-test harness: its stub `classList.toggle` now honors the force argument like the real DOM, so tab/pane show-hide logic is actually verified. cache -> `scob-sky-v42`. |
| **v3.37** | `sky-finder.html`, `tonights-tour.html`, `identify.html`→stub, `audio-tour.html`→stub, `sky-guide.html`, `scob-dashboard-v3.html`, `version.js`, `sw.js` | **Navigation tidy-up: two near-duplicate pages merged into their siblings.** **Point your phone** (`sky-finder.html`) now has a **Find / Identify** toggle — Find steers an arrow to a target as before, Identify names the brightest object you're aiming at — so the old separate *What am I looking at?* page is gone, folded into the same compass page; `identify.html` is now a redirect to `sky-finder.html?mode=identify` so printed QRs and installed apps keep working, and the object-card deep-link (`?target=`) is preserved. **Tonight's Tour** gained a **🔊 Play all** button that reads the whole tour hands-free with auto-advance and stop-highlighting, replacing the standalone audio-tour page; `audio-tour.html` now redirects to the tour. The public **Sky Guide drops from 13 tiles to 11** and the dashboard's *Tonight's session* menu group from 6 links to 4 — fewer choices to scan, nothing removed. cache -> `scob-sky-v41`. |
| **v3.36** | `scob-dashboard-v3.html`, `version.js`, `sw.js` | **Two small conveniences for volunteers.** A new **📣 Share blurb** button generates a ready-to-paste one-line announcement for WhatsApp, Telegram or the SCOB Facebook — e.g. *“Free public stargazing at the Science Centre Observatory this Friday (24 Jul), 7:30–10pm. Clear skies are forecast… come look at Venus and the gibbous Moon.”* — built live from the go/no-go verdict and the actual bright sights up that evening, in an editable pop-up with one-tap copy (and the native share sheet on mobile). Fills the weekly job of writing that post by hand; it's the text twin of the existing highlights image card. A dismissible **“New in vX” line** now greets returning volunteers on the dashboard, summarising what changed since they last looked and offering to open the menu, remembered per version so it shows once. cache -> `scob-sky-v40`. |
| **v3.35** | `scob-dashboard-v3.html`, `version.js`, `sw.js` | **Collapsible menu — the drawer no longer scrolls forever.** The ☰ menu's section headers (Tonight's session / Explore & share / Reference guides / Planning & setup) are now tap-to-expand accordions: the drawer opens as a short list of headers with only **Tonight's session** expanded — the group a docent needs mid-session — instead of a 26-item scroll. Each section's open/closed state is remembered per visitor. A new **🔍 filter box** at the top live-hides everything that doesn't match what you type (auto-expanding the sections with hits and showing a 'no match' note when nothing does), so a known target is one keystroke away. The Public Sky Guide link and the accessibility toggle stay pinned outside the collapsing groups. Wired in the shared desktop/mobile script so both layouts get it. cache -> `scob-sky-v39`. |
| **v3.34** | `seestar.html` (new), `sky-guide.html`, `scob-dashboard-v3.html`, `version.js`, `sw.js` | **The Seestar page, and the public front door finally reaches everything.** **`seestar.html` (new)** explains SCOB's Seestar S30 smart telescope to visitors: why live-stacking lets a 30 mm robot show glowing colour nebulae the 40 cm dome can't — it keeps adding light up (10-second frames stacked over minutes) where the eye takes a fresh throwaway exposure 15x a second and is colour-blind in the dark. Covers the two real advantages under a city sky (built-in dual-narrowband Hα/OIII light-pollution filter; a wide ~2.1° x 1.2° field that frames whole objects the domes can't), the honest limits (broadband galaxies and tiny planets are not its game), an interactive noise-vs-exposure stacking illustration, a live 'good Seestar target now' that prefers emission nebulae, an up-now target table with frame-fit, and docent notes. Specs verified against the ZWO manufacturer FAQ. **Sky Guide wiring** — the public `sky-guide.html` now surfaces the pages that were previously only in the volunteer drawer: the Seestar, phone astrophotography, 'why Singapore sees the whole sky', and the light-pollution explainer, each as a fully four-language tile (EN/中文/BM/Tamil). Closes the gap where three visitor pages were unreachable from the visitor home screen. cache -> `scob-sky-v38`. |
| **v3.33** | `singapore-sky.html` (new), `light-pollution.html` (new), `phone-astrophotography.html` (new), `test-pages.js` (new), `test-astro.js`, `scob-dashboard-v3.html`, `.github/workflows/deploy-pages.yml`, `check-release.sh`, `version.js`, `sw.js` | **A maintenance release with three new visitor pages.** **Quality first: `test-pages.js` (new)** executes every one of the 26 HTML pages against a stub DOM with the network offline and fails the build on any crash, missing element id, duplicate id, broken local link, unhandled promise rejection or missing version plumbing — dependency-free, so it runs in CI with no network. It immediately found dead code in the dashboard's view-switcher (a lookup for a `mobileScript`/`desktopScript` element that no longer exists), now removed. Wired into the deploy workflow alongside the astronomy tests. **Review guards** in `test-astro.js` now fail the build when dated data goes stale — comet orbital elements and the Great Red Spot longitude each carry a review date, and the build warns 30 days out and fails once overdue past its grace period. **`singapore-sky.html` (new) — why Singapore sees almost the whole sky.** From 1.3342 deg N only a 2.7 deg circle around the south celestial pole never rises: **99.99% of the entire sky is reachable over a year**, against 97% from Mauna Kea, 95% from Paranal and 81% from London. Computed live, with a declination-band chart, a 14-object comparison table across six observatories with live current altitudes, the both-hemispheres pairs (Polaris *and* the Southern Cross; the Big Dipper *and* the Magellanic Clouds), and an honest section on the difference between visible and observable — Polaris culminates at 2.1 deg, and Sigma Octantis misses by a third of a degree. **`light-pollution.html` (new) — where did all the stars go?** Two simulated panels of the *same* star field, dark site versus Jurong, with the star count falling from 585 to 59 at Bortle 8; the key idea that light pollution raises the floor rather than hiding the sky, an honest target-by-target table of what a bright sky does and does not cost, and what to tell a disappointed visitor. Shares the dashboard's Bortle setting. **`phone-astrophotography.html` (new)** — a live "best phone target right now", a realistic five-tier ranking of what a phone can achieve, step-by-step recipes for the Moon and for wide-field night mode, and the three deck rules (no flash, dim the screen, never touch the telescope). cache -> `scob-sky-v37`. |
| **v3.32** | `scale-model.html`, `version.js`, `sw.js` | **SCOB's own 3-metre inflatable Sun as a scale preset** — now the default on the solar-system-to-scale page, so the demonstration can be run with the prop the visitors are actually standing next to. At that scale the Earth is a 2.7 cm walnut **322 m away** (about three football pitches from the inflatable), Jupiter a 30 cm beach ball 1.7 km off, and Neptune a tennis ball **9.7 km** down the road — with a closing line that the nearest other star would sit 86,600 km away, more than twice around the Earth. The page adds a docent note making the point explicitly: a Sun you can touch, planets that will not fit on the site. Also tightened the everyday-object size comparisons against real diameters (ping-pong 40 mm, tennis 67 mm, grapefruit ~120 mm) by adding 'large marble' and 'walnut' bands — the classic basketball-Sun / peppercorn-Earth at 25.8 m is unchanged. cache -> `scob-sky-v36`. |
| **v3.31** | `iss-transits.html` (new), `astro-core.js`, `eyepiece-fov.html`, `scale-model.html`, `constellation-story.html`, `scob-dashboard-v3.html`, `occultations.html`, `version.js`, `sw.js` | **The last five queued ideas, shipped together.** **`iss-transits.html` (new)** predicts the seconds when the ISS or Tiangong crosses the *face* of the Sun or Moon from SCOB's exact coordinates — a two-stage search (60 s sweep for passes, then a 10 s scan refined by ternary search to ~1 ms) over SGP4 orbits from CelesTrak, reporting the SGT time to a tenth of a second, chord duration, the station's apparent size as a percentage of the disc, a diagram of where the chord crosses, and near misses worth re-checking. Carries a prominent solar-filter safety warning and an honest note that the ground path is only kilometres wide and TLEs age fast. **Eyepiece recommender** — `eyepiece-fov.html` now ranks the whole eyepiece case for the chosen target and scope, ruling out anything past 2x aperture or ~250x (Singapore seeing) or over a 7 mm exit pupil, then scoring on framing, image scale and — for faint objects under Jurong's sky — exit pupil (~3 mm keeps the background darkest); suggests a Barlow when the case falls short, and knows the Seestar takes no eyepiece at all. **Cosmic distance ladder** on `scale-model.html` — an interactive six-rung ladder (radar ranging → parallax → spectroscopic → Cepheids → Type Ia supernovae → redshift) on a log-scale distance bar, each rung showing what calibrates it, ending on the Hubble tension. **3D-view snapshot** — a 📸 Save picture button on the constellation page renders the current depth or galaxy view to a captioned PNG. **Sky-map cross-link** — constellation names on the dashboard sky map are now tappable and open that constellation's story and 3D view. Engine: `astro-core.js` gains `sunTopo`, `moonDisc`, `angSep` and `transitScan`; CI gains 15 transit-geometry checks against an analytically-known synthetic track. cache -> `scob-sky-v35`. |
| **v3.30** | `constellation-story.html`, `version.js`, `sw.js` | Two more depth ideas on the constellation page. **"You're seeing the past"** — using each star's distance, it names the year the light you're looking at actually set out (e.g. Alnilam's left around 26 BC and is only reaching your eye tonight). **Constellations aren't permanent** — a time slider runs the sky ±100,000 years using real proper motions, so the nearby stars visibly drift and the familiar pattern warps, with a ⏱ Today reset. Verified against published proper motions: over 100,000 yr Alpha Centauri moves ~102°, Arcturus ~63°, Sirius ~37°, Dubhe ~4° (the classic Big Dipper deformation) while distant stars like Betelgeuse barely shift; all 16 constellations stay in frame across the full time and pan range. Distant stars default to negligible motion, which is physically right. cache → `scob-sky-v34`. |
| **v3.29** | `constellation-story.html`, `version.js`, `sw.js` | Centaurus data fix: a connecting line had been dropped, leaving Menkent drawn as a detached dot (it looked like a missing star); two stars were also mislabelled (Epsilon Centauri had been named "Menkent", and the real Menkent "Menkent B"), which gave Epsilon Centauri the wrong distance (61 ly instead of ~430 ly) in the 3D view. Names, the missing line and both distances corrected. Added an automated check across **all 16 constellations** for orphan stars, out-of-range line indices and stars missing a distance — all clean. cache → `scob-sky-v33`. |
| **v3.28** | `constellation-story.html`, `version.js`, `sw.js` | **"Where in the galaxy" + a view from above.** Each constellation now gets a computed galactic-position line — converting its stars to galactic coordinates (l, b) to say whether you're looking *in toward the heart of the Milky Way* (Scorpius, Sagittarius), *outward to the anticentre* (Orion, Taurus), or *along our own spiral arm* (Cygnus), and whether it sits in the galactic plane (thick star fields) or steeply out of it (Leo, Virgo, Boötes — the window through which distant galaxies show). It also makes the honest point that **every naked-eye star here is in our own Orion Arm**, spanning a few thousand light-years of a 100,000 ly galaxy. A new **🌌 View from above** mode swaps the sky chart for a top-down map of the galactic plane — the Sun at centre, distance rings, the constellation's stars at their true galactic positions, and an arrow toward the galactic centre — showing visually how the stars trail away along one line of sight. Verified: galactic conversion lands Sgr A* at l=0°, b=0°, and all stars stay in frame in both views for all 16 constellations. cache → `scob-sky-v32`. |
| **v3.27** | `constellation-story.html`, `version.js`, `sw.js` | Fix for the 3D pan: corrected a vertical-centering sign error and the near-star depth handling that let some stars drift outside the frame while panning. Verified programmatically that **all 16 constellations keep every star inside the view across the full pan range**, and the swing is capped so the pattern spreads out cleanly without stars escaping. cache → `scob-sky-v31`. |
| **v3.26** | `constellation-story.html`, `version.js`, `sw.js` | **"Fly around the constellation" — a 3D depth reveal.** The featured constellation's diagram is now interactive: drag it sideways (or use the 👁 Earth → 🚀 Side slider) and a perspective camera orbits out from Earth's viewpoint, so the flat star pattern **pulls apart into 3D and shows the stars are at wildly different distances** — answering the classic visitor question ("aren't all these stars close together?"). Real distances (light-years) drive it, with warm-to-cool colouring by distance and live distance labels (e.g. Orion: Betelgeuse 548 ly vs Alnilam 2,000 ly, which only *look* adjacent). Fully on-device. cache → `scob-sky-v30`. |
| **v3.25** | `sky-guide.webmanifest` (new), `sky-guide.html`, `qr-poster.html`, `version.js`, `sw.js` | **Install / QR entry point for the Sky Guide.** The guide is now an installable PWA (`sky-guide.webmanifest`, `start_url` = the guide, standalone display, maskable icons): it shows an **"Install this app"** button on Android/Chrome (via `beforeinstallprompt`), an "Add to Home Screen" hint on iPhone, and a **QR code that opens the guide** (offline-cached QR library) so visitors can scan it from a poster and take SCOB home. The install prompt/labels are translated into all four languages. The printable **QR poster** now offers the Sky Guide as its default target. cache → `scob-sky-v29`. **Still to do for this project:** the 4-language rollout into the five linked tool pages (finder, tour, identify, constellation stories, almanac) — a dedicated content-translation pass (mechanism proven by the kiosk and guide). |
| **v3.24** | `sky-guide.html` (new), `scob-dashboard-v3.html`, `version.js`, `sw.js` | **New project — "SCOB Sky Guide", the public front door.** A polished, mobile-first, multilingual (EN / 中文 / Bahasa Melayu / தமிழ்) home screen for visitors that ties the public tools into one branded experience: a hero "tonight at a glance" (sky state, and the standout sight right now with its illustration, computed on-device) over tidy tiles grouped into *See the sky tonight* (tour, point-your-phone finder, "what am I looking at?"), *Learn & explore* (constellation stories, audio tour, almanac) and *Just for fun* (solar-system-to-scale, quiz, daytime/Sun). Marked `translate="no"` so Chrome leaves the chosen language alone; works offline once loaded. This is the seed of a consolidated visitor app — next steps: extend full multilingual into the linked tool pages, and an install/QR entry point. Linked from the dashboard ☰ menu. cache → `scob-sky-v28`. |
| **v3.23** | `occultations.html`, `almanac.html`, `version.js`, `sw.js` | "Visible from SCOB" filters, default on. The **occultations** page now hides, by default, events that happen with the Moon below the horizon or in daylight here — showing only what's actually observable from Singapore, with a one-tap "show all" to reveal the rest. The **almanac's meteor-shower** list likewise hides showers whose radiant climbs too low from our near-equatorial latitude (e.g. the Ursids, radiant only ~15° up) by default, showing each shower's radiant altitude and a "show all" link. Both remember the choice. Also added libration + occultation + topocentric checks to the CI regression test. cache → `scob-sky-v27`. |
| **v3.22** | `astro-core.js`, `occultations.html` (new), `almanac.html`, `scob-dashboard-v3.html`, `version.js`, `sw.js` | The two hard ones, done properly. **Lunar libration** — the almanac's Moon card now reports how the Moon is tipped (Meeus optical libration): how many degrees toward the eastern/western and northern/southern limbs, which extra terrain that reveals, and a "near maximum tonight" flag for the best limb-feature nights. **`occultations.html` — lunar occultation predictor** scans the next ~5 months for the Moon covering (or closely passing) the bright zodiac stars Aldebaran, Regulus, Spica and Antares and the planets, using the Moon's **topocentric, parallax-corrected** position for SCOB — essential for occultations. Each event shows the SGT time, whether it's an occultation or a close pass, and whether it's actually visible from Singapore (Moon up and sky dark), with the genuinely-visible ones highlighted. Validated: reproduces the real 2026 Regulus & Antares occultation series, with correct Moon parallax (0.98°) and angular radius (0.267°). cache → `scob-sky-v26`. |
| **v3.21** | `almanac.html` (new), `quiz.html` (new), `scob-dashboard-v3.html`, `version.js`, `sw.js` | Two more engagement pages. **`almanac.html` — Sky Almanac** shows tonight's Moon phase, the planets on show, the **Milky-Way-core window** (the dark, high span when the galactic centre in Sagittarius is up — computed live, with a Moon-glare note), the meteor showers peaking this month, and a "this month in the sky" highlight. **`quiz.html` — Constellation Quiz** draws a real star pattern and asks you to name it (four choices, running score) — a fun way to learn the sky, using the same bright-star asterisms as the constellation pages. Both linked from the ☰ menu. cache → `scob-sky-v25`. Note: multilingual finder/tour remains planned for a later release. |
| **v3.20** | `identify.html` (new), `audio-tour.html` (new), `scob-dashboard-v3.html`, `version.js`, `sw.js` | Two new public-engagement pages. **`identify.html` — "What am I looking at?"** is the reverse of the finder: point your phone at the sky and it names the brightest object in that direction (Sun, Moon, planets, bright stars, clusters, comets), with the angular distance from your aim and nearby runners-up. It reuses the finder's locked-north compass + tilt handling and computes everything on-device. **`audio-tour.html` — guided audio sky tour** builds tonight's tour in order and reads it out hands-free with the device's speech synthesis (play/pause, next/prev, tap any stop to jump), showing each object's illustration, where to look and talking points as it speaks. Both linked from the ☰ menu. cache → `scob-sky-v24`. |
| **v3.19** | `constellation-story.html`, `version.js`, `sw.js` | **Richer constellation stories** — each constellation now has a full-length telling of its Greek mythology and background (the boastful hunter humbled by a scorpion, Zeus as the bull carrying off Europa, Orpheus's lyre, the vain queen chained to her throne…) woven together with astronomy trivia (Sirius and the Nile calendar, the black hole in the Teapot's spout, Cygnus X-1, the Qixi cowherd-and-weaver tale). A **Short / Full story toggle** (📖, remembered per visitor) switches between a quick blurb and the deep version, and the toggle keeps you on the constellation you're reading. cache → `scob-sky-v23`. |
| **v3.18** | `constellation-story.html` (new), `scale-model.html` (new), `highlights-card.html` (new), `qr-poster.html` (new), `scob-dashboard-v3.html`, `version.js`, `sw.js` | Engagement & reach. Four new pages plus a dashboard tweak: **`constellation-story.html`** features a constellation well-placed over Singapore right now — its myth, a star-hop find guide, best-viewing months, and an accurate asterism diagram (bright-star coordinates), with a list of others up now. **`scale-model.html`** is an interactive "shrink the Sun to a basketball" solar-system-to-scale explainer (model sizes, relatable comparisons and distances). **`highlights-card.html`** generates a downloadable square image of tonight's highlights (Moon phase, best planet, top targets) for posting to social media. **`qr-poster.html`** is a printable A4 "Scan to explore tonight's sky" sign with an offline-capable QR (finder/kiosk/constellation/dashboard). On the **dashboard**, the weather card adds a **cloud-trend arrow** — clearing ↗ / clouding over ↘ / steady → across the 7–11 pm window, from the Open-Meteo hourly forecast. All four pages linked from the ☰ menu. cache → `scob-sky-v22`. |
| **v3.17** | `object-cards.html`, `kiosk.html`, `version.js`, `sw.js` | Fixes. **Object-card icons** — the printable cards were showing the same generic ringed-planet emoji for every planet (so Venus, Jupiter etc. all looked like Saturn); they now use the same on-device `astro-core.js` illustrations as the Tour/finder/kiosk, so each planet, star and deep-sky object is drawn correctly (only Saturn has rings). **Kiosk translation** — the multilingual visitor display now carries `translate="no"` on `<html>` plus a `notranslate` meta tag (re-asserted on every language switch), so Chrome no longer offers to auto-translate the 中文 / Malay / Tamil text back to English. cache → `scob-sky-v21`. |
| **v3.16** | `solar.html`, `version.js`, `sw.js` | **Live solar activity in daytime/solar mode** — the solar page now shows a plain-language activity level plus today's **sunspot regions and spot count**, the largest group's McIntosh/Hale class, the **10.7 cm solar radio flux**, and the **latest X-ray flare** — fetched client-side from the **NOAA Space Weather Prediction Center (SWPC)** with a graceful offline fallback and the source credited in the footer. It also embeds a **live white-light image of the Sun (NASA / SDO HMI)** so you can see today's sunspots directly, refreshing through the session (credited in the footer, offline-safe). Sunspot-region data lags ~a day (reported once daily UTC); flux & flares update through the day. cache → `scob-sky-v20`. |
| **v3.15** | `astro-core.js`, `scob-dashboard-v3.html`, `tonights-tour.html`, `sky-finder.html`, `object-cards.html`, `checklist.html` (new), `version.js`, `sw.js`, `test-astro.js` | Ten upgrades. **Comet tracker** — bright comets (currently 10P/Tempel 2) are computed live from orbital elements in `astro-core.js` and flow into the dashboard target list, Tour, kiosk and finder, with a magnitude estimate and an auto-expiring visibility window; the CI test locks its position against an independent ephemeris. **Rise-to-transit timeline** — each dashboard target shows a bar of tonight's window above 25° with the best (transit) moment marked. **Satellite magnitudes + live countdown** — each ISS/Tiangong/Hubble pass shows an estimated peak brightness, and a live "next pass in 12m, rises SW, mag −2" banner counts down. **Talking-point depth toggle** on the Tour (first-timer hook vs full enthusiast blurb). **Read-aloud** — a 🔊 button narrates each Tour object via the browser's speech synthesis. **Per-object QR deep-links** — the printable object cards carry a QR that opens the finder pre-aimed at that object (`sky-finder.html?target=<id>`). **Setup & pack-down checklist** — a new printable `checklist.html` for the 40 cm dome, CPC/C6 deck scopes and Seestar. **Session pack** — the printable briefing now also includes talking-point cards for tonight's showpieces. **Update-available toast** — installed PWAs show a one-tap "New version — refresh" prompt when a fresh build is cached. **Milky Way band + constellation toggle** on the sky map. cache → `scob-sky-v19`. |
| **v3.14** | `astro-core.js`, `scob-dashboard-v3.html`, `tonights-tour.html`, `kiosk.html`, `sky-finder.html`, `test-astro.js` (new), `.github/workflows/deploy-pages.yml`, `version.js`, `sw.js` | **One astronomy engine, everywhere** — the dashboard's own copy of the position math (Sun, Moon, planets, ecliptic→equatorial, planet magnitudes) was retired and now **delegates to the shared `astro-core.js`**, so there is a single validated engine to maintain instead of two that could silently drift. A new **`test-astro.js` CI regression test** locks the engine to independently-verified reference positions (sidereal time, Sun/Moon/planet altitudes & magnitudes, twilight times) and runs automatically on every deploy — a bad edit now fails the build instead of shipping. **Object illustrations** — the Tonight's Tour, kiosk and sky-finder cards now show a small, recognisable on-device drawing of each object (Moon with craters & terminator, planets with Saturn's rings / Jupiter's bands, nebula glows, spiral galaxies, globular vs open clusters, coloured stars & doubles). They're generated as lightweight SVG in `astro-core.js` — no image files, so they work fully offline. cache → `scob-sky-v18`. |
| **v3.13** | `scob-dashboard-v3.html`, `plan-ahead.html`, `solar.html`, `object-cards.html` (new), `version.js`, `sw.js` | **Accessibility mode** on the dashboard (☰ menu toggle) — larger text, a colour-blind-safe verdict symbol on every tag (●/◑/○/✕), higher-contrast secondary text, and an ARIA label on the sky map. **Meteor-shower radiant** — near a major shower's peak the sky map draws the radiant with a Singapore-latitude-adjusted rate. Three new pages: **`plan-ahead.html`** compares the next ~6 session Fridays by Moon phase + Open-Meteo forecast and flags the best night; **`solar.html`** a daytime/solar mode (live Sun position, safe-viewing checklist, what to look for); **`object-cards.html`** printable one-per-object handout cards for the observing deck. All linked from the ☰ menu. cache → `scob-sky-v17`. |
| **v3.12** | `astro-core.js`, `kiosk.html`, `tonights-tour.html`, `version.js`, `sw.js` | **Much broader showpiece catalogue** — the shared `SHOWPIECES` list grew from ~19 to **42**, spread across all right-ascensions so the kiosk, Tonight's Tour and finder always have plenty of targets in **every season, year-round** (typically 14–23 objects up at 9 pm). Added **Mercury, Uranus and Neptune**, plus all-season deep-sky/stars (Beehive, Double Cluster, M22, Swan/Ring/Dumbbell nebulae, M6/M11 clusters, 47 Tuc, and bright stars Vega, Altair, Deneb, Arcturus, Spica, Capella, Rigel, Betelgeuse, Regulus, Procyon, Fomalhaut). Planet selection is now generic (by object kind), and the kiosk carries Chinese names for the new objects. Positions are all still computed live — **nothing needs updating over time**. cache → `scob-sky-v16`. |
| **v3.11** | `sky-finder.html`, `scob-dashboard-v3.html`, `kiosk.html`, `align-stars.html` (new), `version.js`, `sw.js` | **Finder up/down guidance** — the point-your-phone finder now uses the phone's tilt to say "raise/lower phone" and confirm "✔ on target", with a live "where your phone is aimed" marker (plus the earlier compass fix: single locked north source + smoothing, no more 90° flicker). **Darkness-window bar** on the dashboard — a slim timeline of sunset → astronomical dark → moonrise/set → dawn with the usable dark window shaded and the Moon-up band marked. **Offline-capable kiosk QR** — the QR library is now cached in the browser on first load so the code renders with no internet afterwards. **Multilingual kiosk** — a EN / 中文 / Malay / Tamil toggle for the visitor display (Malay & Tamil are machine translations pending a native-speaker review). New **`align-stars.html`** (volunteer) — suggests bright, well-separated alignment stars that are up now for the CPC GoTo mounts (SkyAlign / 2-star). cache → `scob-sky-v15`. |
| **v3.10** | `kiosk.html` (new), `scob-dashboard-v3.html`, `version.js`, `sw.js` | **`kiosk.html`** — a full-screen, auto-refreshing visitor display for the observatory: a live clock, the sky state (daytime / twilight / dark), the Moon phase, and the top few objects up *right now* in plain language, plus a big **QR code** that visitors scan to open the point-your-phone finder on their own device (the QR resolves to `sky-finder.html` on whatever host it's served from; it degrades to a printed URL if the QR library or connection is unavailable). Linked from the dashboard. **Tidier navigation** — the companion-page links are consolidated into a single **☰ Menu** drawer (top-left of the dashboard, grouped into "Tonight's session" and "Reference guides") instead of a long row of buttons. The **data-health strip** was reworded to plain language ("🩺 Live-data freshness — weather forecast: up to date · satellite pass data: fresh · Jupiter red-spot model: good to Jan 2027") with hover tooltips. cache → `scob-sky-v14`. |
| **v3.9** | `astro-core.js` (new), `tonights-tour.html` (new), `sky-finder.html` (new), `eyepiece-fov.html` (new), `scob-dashboard-v3.html`, `sw.js` | Three new companion pages plus a dashboard add-on: **`tonights-tour.html`** — an auto-generated, printable docent tour that lists what to show the public *in order* with plain-language talking points and the right SCOB scope for each object; **`sky-finder.html`** — a "point-your-phone" finder that uses the device compass to steer you to a chosen showpiece (with a text direction/altitude fallback for phones without sensors); **`eyepiece-fov.html`** — an eyepiece/field-of-view calculator that frames a target to scale for any SCOB scope + eyepiece (magnification, true field, exit pupil, useful-magnification warnings). On the **dashboard**, an optional **satellite-track overlay** draws tonight's ISS/Tiangong/Hubble pass arcs on the sky map (toggle, off by default). A new shared **`astro-core.js`** engine (Schlyter position math, validated arc-minute-accurate against an independent NOAA computation) powers the tour & finder so there's one engine to maintain. cache → `scob-sky-v13`. |
| **v3.8** | `scob-dashboard-v3.html`, `version.js`, `sw.js` | **Tap-to-locate on the sky map** — clicking any object highlights it on the all-sky chart with a pulsing gold ring + label, scrolls the map into view, and shows a locator note ("42° up in the SE"), with a graceful "below the map / not dark yet" message when it can't be plotted. The highlight follows the object as you scrub the time slider. Works from the **deep-sky targets list, the Solar-System table (planets, Moon, Sun) and the double/carbon-star list**. cache → `scob-sky-v12`. |
| **v3.7** | `scob-dashboard-v3.html`, `version.js`, `sw.js` | Five observing upgrades: **seeing & transparency** in the weather card (7Timer! astro forecast when online, with an offline estimate from the loaded Open-Meteo data as fallback) · **light-pollution-honest deep-sky verdicts** — a Bortle selector (default Bortle 8, typical Jurong) trims faint galaxies/nebulae the city sky won't actually show · **per-target observing windows** — each target shows when it's above 25° during the session and its best time, not just the current altitude · **live "Now" session screen** — a full-screen, auto-refreshing, red-mode-friendly view giving the best target per instrument at the current minute, for use at the eyepiece · **data-health strip** — surfaces forecast age, orbital-element (TLE) age and the GRS review-by date so stale inputs are visible · **compact, collapsible lists** — the deep-sky targets and the double/carbon stars now preview the top 5 (ranked easiest-to-hardest) with a "Show all / Show fewer" toggle, remembered per list, so the page stays short to scroll. Sources for the new feeds credited in every footer (seeing/transparency — 7Timer!; light-pollution realism — Bortle scale). cache → `scob-sky-v11`. |
| **v3.6** | `version.js` (new), `check-release.sh` (new), all pages, `sw.js` | **Single source of truth for the version** — new `version.js` holds one `SCOB_VERSION` constant and injects the `SCOB Night-Sky vX.Y · © AFBOOSTER.cc` footer into every page at runtime (via a `.scob-version` placeholder), so a release now means editing **one** number instead of ~10 (also retires the twin-script-block footer duplication) · **`check-release.sh` pre-flight script** — verifies version parity, that every page loads `version.js`, that `sw.js` caches all pages + `version.js`, that the README marks the current version, and that internal links resolve · **dated review marker** on the Jupiter GRS longitude constant (`REVIEW BY 2027-01-01`) so the drift fit gets re-checked before it silently goes stale · cache → `scob-sky-v10`. |
| **v3.5** | `tracking-methods.html`, plus version-footer bump across `main.html`, `scob-dashboard.html`, `scob-dashboard-v3.html`, `moon-tonight.html`, `telescope-types.html`, `sw.js` | **Removed StarSense AutoAlign** from the tracking & alignment methods page — SCOB has no StarSense module in its Celestron collection, so it's no longer listed as an alignment method (method card, at-a-glance table, meta description and source credit all scrubbed; plate-solving still explained generically for the Seestar) · site-wide version footer synced to **v3.5** · cache → `scob-sky-v9`. |
| **v3.4** | `tracking-methods.html`, `scob-dashboard-v3.html`, `sw.js` | **Single shared script** — the duplicated desktop/mobile script blocks merged into one `sharedScript` (layout detected at runtime; halves maintenance) · **in-page MSS 2-hr Nowcast** (Jurong East, data.gov.sg) in the weather card · **sky map embedded in the printable briefing** · **auto night mode** (red theme switches on after sunset until manually overridden) · new **tracking & alignment methods page** (Celestron SkyAlign / 1-star / 2-star / Auto 2-star / Solar System / ASPA, tracking rates, SCOB mount comparison), cross-linked & cached · cache → `scob-sky-v8`. |
| **v3.3** | `moon-tonight.html`, `scob-dashboard-v3.html`, `sw.js` | Added: **Tonight on the Moon page** (computed terminator + feature guide, cross-linked) · **Jupiter/Saturn eyepiece detail** (Galilean moons, GRS transit times, ring tilt) · **double & carbon star list** (18 moon-proof targets) · **on-device conjunction finder** (Moon–planet & planet–planet, 3 weeks ahead) · **embedded MSS rain radar** in the weather card · **Tiangong & Hubble passes** alongside the ISS · briefing includes all new sections · cache → `scob-sky-v7`. |
| **v3.2** | `scob-dashboard-v3.html`, `sw.js`, `archive/` | Mobile: **slim sticky header** (compact date/time/slider bar) · **special-events calendar 2026–27** (showers, eclipses, oppositions — SG-visibility flagged) · **ISS evening pass predictions** (CelesTrak TLE + satellite.js, on-device) · **live NEA PSI/haze** in the weather card · **auto session run-sheet** per instrument at 7:30/8:30/9:30 pm · **📄 printable briefing** · data-source credits in the footer · old entry points archived, `main.html`/`scob-dashboard.html` now redirect · cache → `scob-sky-v6`. |
| **v3.1** | `telescope-types.html`, `scob-dashboard-v3.html`, `sw.js` | Added the **telescope guide page** (animated optics for 5 telescope types, 4 mount types, DSO-tracking explainer, magnification calculator with SCOB fleet presets) · **cross-links** between dashboard footer and guide · guide added to the **offline cache** (`sw.js` → `scob-sky-v5`). |
| **v3** | `scob-dashboard-v3.html` | Added: **auto-loading weather** (fetches on open and on date change, cached, button now just refreshes) · **evening cloud timeline** (6–11 pm hourly bars, clearest window highlighted) · **sunset / fully-dark / sunrise times** computed on-device · **tonight-at-a-glance summary card** (verdict + moon + best planet + top target). |
| **v2.1** | `scob-dashboard.html`, `main.html` | Merged desktop + mobile into **one auto-detecting file** with a manual view switcher; added **PWA support** (installable, offline service worker). |
| **v2** | `scob-dashboard-desktop.html`, `scob-dashboard-mobile.html` | Added **live weather**: button-triggered Open-Meteo forecast for the chosen date & time with a GO / CAUTION / NO-GO verdict. |
| **v1** | `scob-dashboard-desktop_WO_Weather_Old_Version.html`, `scob-dashboard-mobile_WO_Weather_Old_Version.html` | Original release: separate desktop & mobile pages. Sky map, moon, solar system, deep-sky targets with Visual/Seestar verdicts, seasonal weather notes only (no live forecast). |

### Cutting a release

The version number lives in **one** place now. To ship a change:

1. Edit `SCOB_VERSION` in **`version.js`** (every page's footer updates from it automatically).
2. Bump the `CACHE` name in **`sw.js`** (e.g. `scob-sky-v10` → `v11`) — this is what forces installed PWAs to pull the new pages.
3. Add a row to the version-history table above and move the marker.
4. Run the test suites — **`node test-astro.js`** (engine + dated-data review guards), **`node test-pages.js`** (every page loads & runs offline without crashing), **`node test-logic.js`** (the interactive decision logic — eyepiece recommender, Seestar frame-fit, finder Identify, distance-ladder formatting, share blurb). All three also run in CI on every deploy.
5. Run **`./check-release.sh`** — it fails loudly if anything is out of sync.
6. Upload the changed pages plus `sw.js`, `version.js`, and `README.md`.

## Other files in this folder

- **telescope-types.html** — the telescope types, mounts & magnification guide (linked from the dashboard).
- **tracking-methods.html** — how tracking mounts & GoTo alignment work (Celestron methods focus).
- **moon-tonight.html** — Tonight on the Moon: terminator features for the chosen night (linked from the dashboard).
- **tonights-tour.html** — auto-generated docent tour: what to show the public in order, with talking points (linked from the dashboard).
- **sky-finder.html** — "point-your-phone" compass finder for tonight's showpieces.
- **eyepiece-fov.html** — eyepiece / field-of-view calculator with to-scale target framing.
- **filters.html** — imaging &amp; visual filters reference (broadband, narrowband, dual-band, LRGB, planetary, lunar &amp; solar), with a decision guide and cheat sheet (linked from the dashboard Reference guides menu).
- **kiosk.html** — full-screen visitor display (multilingual) with a QR code to the point-your-phone finder.
- **align-stars.html** — volunteer GoTo alignment-star picker for the CPC mounts.
- **plan-ahead.html** — compares upcoming session Fridays (Moon + forecast) to pick the best night.
- **solar.html** — daytime / solar-observing mode (Sun position, safety, what to look for).
- **object-cards.html** — printable per-object handout cards for the observing deck.
- **astro-core.js** — shared on-device astronomy engine used by the tour & finder pages.
- **Telescopes-Reference.md** — notes on the telescopes available at SCOB.
- **version.js** — single source of truth for the site version; injects the version/copyright footer into every page. Bump `SCOB_VERSION` here on each release.
- **check-release.sh** — pre-flight check; run `./check-release.sh` before uploading to confirm everything is in sync.
- **test-astro.js / test-pages.js / test-logic.js** — the three regression suites (astronomy engine · page smoke tests · interactive logic). Dependency-free Node; run before every release and in CI. Not web assets — do not upload.
- **INSTALL-as-app.md** — how to install the dashboard as a home-screen app.

## Cadence

The dashboard is a living app — open `scob-dashboard-v3.html` any time and it computes the coming Friday's session on the spot; if a docent wants a printout, the built-in **📄 Session pack** button generates one on demand. (The old standalone weekly `Night-Sky-Briefing-*` files and their scheduled generator were retired on 2026-07-21, since the dashboard replaced the need for them.)

## Data sources

Deep-sky catalogue: AstroPixels Messier & Caldwell catalogs (F. Espenak) · planetary orbital elements: P. Schlyter · weather: Open-Meteo & MSS weather.gov.sg · seeing/transparency: 7Timer! astro · light-pollution realism: Bortle scale · haze: NEA via data.gov.sg · sky events: Sea & Sky astronomy calendar (seasky.org) · ISS/Tiangong/Hubble orbits: CelesTrak TLEs, propagated with satellite.js · Jupiter/Saturn physical ephemeris: J. Meeus, Astronomical Algorithms · GRS longitude: JUPOS trend · lunar feature positions: IAU/USGS Gazetteer of Planetary Nomenclature · double/carbon star data: WDS & SIMBAD (compiled) · rain radar: MSS weather.gov.sg · Science Centre Observatory.

**Project convention: every page credits its data sources in the footer** — keep this when adding new data feeds.
